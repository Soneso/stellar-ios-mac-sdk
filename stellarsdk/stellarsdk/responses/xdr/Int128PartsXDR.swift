// This file was automatically generated by xdrgen.
// DO NOT EDIT or your changes may be overwritten.

import Foundation

public struct Int128PartsXDR: XDRCodable, Sendable {
  public var hi: Int64
  public var lo: UInt64

  public init(hi: Int64, lo: UInt64) {
    self.hi = hi
    self.lo = lo
  }

  public init(from decoder: Decoder) throws {
    var container = try decoder.unkeyedContainer()
    hi = try container.decode(Int64.self)
    lo = try container.decode(UInt64.self)
  }

  public func encode(to encoder: Encoder) throws {
    var container = encoder.unkeyedContainer()
    try container.encode(hi)
    try container.encode(lo)
  }
}

extension Int128PartsXDR {
  public func toTxRep(prefix: String, lines: inout [String]) throws {
    lines.append("\(prefix).hi: \(self.hi)")
    lines.append("\(prefix).lo: \(self.lo)")
  }

  public static func fromTxRep(_ map: [String: String], prefix: String) throws -> Int128PartsXDR {
    let hi: Int64 = try TxRepHelper.parseInt64(TxRepHelper.getValue(map, "\(prefix).hi") ?? "0")
    let lo: UInt64 = try TxRepHelper.parseUInt64(TxRepHelper.getValue(map, "\(prefix).lo") ?? "0")
    return Int128PartsXDR(hi: hi, lo: lo)
  }
}

extension Int128PartsXDR: XdrJsonCodable {
  public func toXdrJsonValue() throws -> XdrJsonValue {
    return XdrJson.wideDecimal(
      XdrWideInteger.data128(hi: UInt64(bitPattern: self.hi), lo: self.lo), signed: true)
  }

  public static func fromXdrJsonValue(_ value: XdrJsonValue) throws -> Int128PartsXDR {
    let bytes = try XdrJson.wideDecimal(value, bitSize: 128, signed: true, type: "Int128PartsXDR")
    let parts = XdrWideInteger.parts128(from: bytes)
    return Int128PartsXDR(hi: Int64(bitPattern: parts.hi), lo: parts.lo)
  }
}
