// This file was automatically generated by xdrgen.
// DO NOT EDIT or your changes may be overwritten.

import Foundation

public struct SetOptionsOperationXDR: XDRCodable, Sendable {
  public let inflationDestination: PublicKey?
  public let clearFlags: UInt32?
  public let setFlags: UInt32?
  public let masterWeight: UInt32?
  public let lowThreshold: UInt32?
  public let medThreshold: UInt32?
  public let highThreshold: UInt32?
  public let homeDomain: String?
  public let signer: SignerXDR?

  public init(
    inflationDestination: PublicKey? = nil,
    clearFlags: UInt32? = nil,
    setFlags: UInt32? = nil,
    masterWeight: UInt32? = nil,
    lowThreshold: UInt32? = nil,
    medThreshold: UInt32? = nil,
    highThreshold: UInt32? = nil,
    homeDomain: String? = nil,
    signer: SignerXDR? = nil
  ) {
    self.inflationDestination = inflationDestination
    self.clearFlags = clearFlags
    self.setFlags = setFlags
    self.masterWeight = masterWeight
    self.lowThreshold = lowThreshold
    self.medThreshold = medThreshold
    self.highThreshold = highThreshold
    self.homeDomain = homeDomain
    self.signer = signer
  }

  public init(from decoder: Decoder) throws {
    var container = try decoder.unkeyedContainer()
    let inflationDestinationPresent = try container.decode(Int32.self)
    if inflationDestinationPresent != 0 {
      inflationDestination = try container.decode(PublicKey.self)
    } else {
      inflationDestination = nil
    }
    let clearFlagsPresent = try container.decode(Int32.self)
    if clearFlagsPresent != 0 {
      clearFlags = try container.decode(UInt32.self)
    } else {
      clearFlags = nil
    }
    let setFlagsPresent = try container.decode(Int32.self)
    if setFlagsPresent != 0 {
      setFlags = try container.decode(UInt32.self)
    } else {
      setFlags = nil
    }
    let masterWeightPresent = try container.decode(Int32.self)
    if masterWeightPresent != 0 {
      masterWeight = try container.decode(UInt32.self)
    } else {
      masterWeight = nil
    }
    let lowThresholdPresent = try container.decode(Int32.self)
    if lowThresholdPresent != 0 {
      lowThreshold = try container.decode(UInt32.self)
    } else {
      lowThreshold = nil
    }
    let medThresholdPresent = try container.decode(Int32.self)
    if medThresholdPresent != 0 {
      medThreshold = try container.decode(UInt32.self)
    } else {
      medThreshold = nil
    }
    let highThresholdPresent = try container.decode(Int32.self)
    if highThresholdPresent != 0 {
      highThreshold = try container.decode(UInt32.self)
    } else {
      highThreshold = nil
    }
    let homeDomainPresent = try container.decode(Int32.self)
    if homeDomainPresent != 0 {
      homeDomain = try container.decode(String.self)
    } else {
      homeDomain = nil
    }
    let signerPresent = try container.decode(Int32.self)
    if signerPresent != 0 {
      signer = try container.decode(SignerXDR.self)
    } else {
      signer = nil
    }
  }

  public func encode(to encoder: Encoder) throws {
    var container = encoder.unkeyedContainer()
    if let val = inflationDestination {
      try container.encode(Int32(1))
      try container.encode(val)
    } else {
      try container.encode(Int32(0))
    }
    if let val = clearFlags {
      try container.encode(Int32(1))
      try container.encode(val)
    } else {
      try container.encode(Int32(0))
    }
    if let val = setFlags {
      try container.encode(Int32(1))
      try container.encode(val)
    } else {
      try container.encode(Int32(0))
    }
    if let val = masterWeight {
      try container.encode(Int32(1))
      try container.encode(val)
    } else {
      try container.encode(Int32(0))
    }
    if let val = lowThreshold {
      try container.encode(Int32(1))
      try container.encode(val)
    } else {
      try container.encode(Int32(0))
    }
    if let val = medThreshold {
      try container.encode(Int32(1))
      try container.encode(val)
    } else {
      try container.encode(Int32(0))
    }
    if let val = highThreshold {
      try container.encode(Int32(1))
      try container.encode(val)
    } else {
      try container.encode(Int32(0))
    }
    if let val = homeDomain {
      try container.encode(Int32(1))
      try container.encode(val)
    } else {
      try container.encode(Int32(0))
    }
    if let val = signer {
      try container.encode(Int32(1))
      try container.encode(val)
    } else {
      try container.encode(Int32(0))
    }
  }
}
