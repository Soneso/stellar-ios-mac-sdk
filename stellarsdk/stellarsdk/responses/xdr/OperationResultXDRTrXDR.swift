// This file was automatically generated by xdrgen.
// DO NOT EDIT or your changes may be overwritten.

import Foundation

public enum OperationResultXDRTrXDR: XDRCodable, Sendable {
  case createAccountResult(CreateAccountResultXDR)
  case paymentResult(PaymentResultXDR)
  case pathPaymentStrictReceiveResult(PathPaymentResultXDR)
  case manageSellOfferResult(ManageOfferResultXDR)
  case createPassiveSellOfferResult(ManageOfferResultXDR)
  case setOptionsResult(SetOptionsResultXDR)
  case changeTrustResult(ChangeTrustResultXDR)
  case allowTrustResult(AllowTrustResultXDR)
  case accountMergeResult(AccountMergeResultXDR)
  case inflationResult(InflationResultXDR)
  case manageDataResult(ManageDataResultXDR)
  case bumpSeqResult(BumpSequenceResultXDR)
  case manageBuyOfferResult(ManageOfferResultXDR)
  case pathPaymentStrictSendResult(PathPaymentResultXDR)
  case createClaimableBalanceResult(CreateClaimableBalanceResultXDR)
  case claimClaimableBalanceResult(ClaimClaimableBalanceResultXDR)
  case beginSponsoringFutureReservesResult(BeginSponsoringFutureReservesResultXDR)
  case endSponsoringFutureReservesResult(EndSponsoringFutureReservesResultXDR)
  case revokeSponsorshipResult(RevokeSponsorshipResultXDR)
  case clawbackResult(ClawbackResultXDR)
  case clawbackClaimableBalanceResult(ClawbackClaimableBalanceResultXDR)
  case setTrustLineFlagsResult(SetTrustLineFlagsResultXDR)
  case liquidityPoolDepositResult(LiquidityPoolDepositResultXDR)
  case liquidityPoolWithdrawResult(LiquidityPoolWithdrawResultXDR)
  case invokeHostFunctionResult(InvokeHostFunctionResultXDR)
  case extendFootprintTTLResult(ExtendFootprintTTLResultXDR)
  case restoreFootprintResult(RestoreFootprintResultXDR)

  public init(from decoder: Decoder) throws {
    var container = try decoder.unkeyedContainer()
    let discriminant = try container.decode(Int32.self)

    switch discriminant {
    case OperationType.accountCreated.rawValue:
      let val = try container.decode(CreateAccountResultXDR.self)
      self = .createAccountResult(val)
    case OperationType.payment.rawValue:
      let val = try container.decode(PaymentResultXDR.self)
      self = .paymentResult(val)
    case OperationType.pathPayment.rawValue:
      let val = try container.decode(PathPaymentResultXDR.self)
      self = .pathPaymentStrictReceiveResult(val)
    case OperationType.manageSellOffer.rawValue:
      let val = try container.decode(ManageOfferResultXDR.self)
      self = .manageSellOfferResult(val)
    case OperationType.createPassiveSellOffer.rawValue:
      let val = try container.decode(ManageOfferResultXDR.self)
      self = .createPassiveSellOfferResult(val)
    case OperationType.setOptions.rawValue:
      let val = try container.decode(SetOptionsResultXDR.self)
      self = .setOptionsResult(val)
    case OperationType.changeTrust.rawValue:
      let val = try container.decode(ChangeTrustResultXDR.self)
      self = .changeTrustResult(val)
    case OperationType.allowTrust.rawValue:
      let val = try container.decode(AllowTrustResultXDR.self)
      self = .allowTrustResult(val)
    case OperationType.accountMerge.rawValue:
      let val = try container.decode(AccountMergeResultXDR.self)
      self = .accountMergeResult(val)
    case OperationType.inflation.rawValue:
      let val = try container.decode(InflationResultXDR.self)
      self = .inflationResult(val)
    case OperationType.manageData.rawValue:
      let val = try container.decode(ManageDataResultXDR.self)
      self = .manageDataResult(val)
    case OperationType.bumpSequence.rawValue:
      let val = try container.decode(BumpSequenceResultXDR.self)
      self = .bumpSeqResult(val)
    case OperationType.manageBuyOffer.rawValue:
      let val = try container.decode(ManageOfferResultXDR.self)
      self = .manageBuyOfferResult(val)
    case OperationType.pathPaymentStrictSend.rawValue:
      let val = try container.decode(PathPaymentResultXDR.self)
      self = .pathPaymentStrictSendResult(val)
    case OperationType.createClaimableBalance.rawValue:
      let val = try container.decode(CreateClaimableBalanceResultXDR.self)
      self = .createClaimableBalanceResult(val)
    case OperationType.claimClaimableBalance.rawValue:
      let val = try container.decode(ClaimClaimableBalanceResultXDR.self)
      self = .claimClaimableBalanceResult(val)
    case OperationType.beginSponsoringFutureReserves.rawValue:
      let val = try container.decode(BeginSponsoringFutureReservesResultXDR.self)
      self = .beginSponsoringFutureReservesResult(val)
    case OperationType.endSponsoringFutureReserves.rawValue:
      let val = try container.decode(EndSponsoringFutureReservesResultXDR.self)
      self = .endSponsoringFutureReservesResult(val)
    case OperationType.revokeSponsorship.rawValue:
      let val = try container.decode(RevokeSponsorshipResultXDR.self)
      self = .revokeSponsorshipResult(val)
    case OperationType.clawback.rawValue:
      let val = try container.decode(ClawbackResultXDR.self)
      self = .clawbackResult(val)
    case OperationType.clawbackClaimableBalance.rawValue:
      let val = try container.decode(ClawbackClaimableBalanceResultXDR.self)
      self = .clawbackClaimableBalanceResult(val)
    case OperationType.setTrustLineFlags.rawValue:
      let val = try container.decode(SetTrustLineFlagsResultXDR.self)
      self = .setTrustLineFlagsResult(val)
    case OperationType.liquidityPoolDeposit.rawValue:
      let val = try container.decode(LiquidityPoolDepositResultXDR.self)
      self = .liquidityPoolDepositResult(val)
    case OperationType.liquidityPoolWithdraw.rawValue:
      let val = try container.decode(LiquidityPoolWithdrawResultXDR.self)
      self = .liquidityPoolWithdrawResult(val)
    case OperationType.invokeHostFunction.rawValue:
      let val = try container.decode(InvokeHostFunctionResultXDR.self)
      self = .invokeHostFunctionResult(val)
    case OperationType.extendFootprintTTL.rawValue:
      let val = try container.decode(ExtendFootprintTTLResultXDR.self)
      self = .extendFootprintTTLResult(val)
    case OperationType.restoreFootprint.rawValue:
      let val = try container.decode(RestoreFootprintResultXDR.self)
      self = .restoreFootprintResult(val)
    default:
      throw StellarSDKError.xdrDecodingError(message: "Unknown OperationResultXDRTrXDR discriminant: \(discriminant)")
    }
  }

  public func type() -> Int32 {
    switch self {
    case .createAccountResult: return OperationType.accountCreated.rawValue
    case .paymentResult: return OperationType.payment.rawValue
    case .pathPaymentStrictReceiveResult: return OperationType.pathPayment.rawValue
    case .manageSellOfferResult: return OperationType.manageSellOffer.rawValue
    case .createPassiveSellOfferResult: return OperationType.createPassiveSellOffer.rawValue
    case .setOptionsResult: return OperationType.setOptions.rawValue
    case .changeTrustResult: return OperationType.changeTrust.rawValue
    case .allowTrustResult: return OperationType.allowTrust.rawValue
    case .accountMergeResult: return OperationType.accountMerge.rawValue
    case .inflationResult: return OperationType.inflation.rawValue
    case .manageDataResult: return OperationType.manageData.rawValue
    case .bumpSeqResult: return OperationType.bumpSequence.rawValue
    case .manageBuyOfferResult: return OperationType.manageBuyOffer.rawValue
    case .pathPaymentStrictSendResult: return OperationType.pathPaymentStrictSend.rawValue
    case .createClaimableBalanceResult: return OperationType.createClaimableBalance.rawValue
    case .claimClaimableBalanceResult: return OperationType.claimClaimableBalance.rawValue
    case .beginSponsoringFutureReservesResult: return OperationType.beginSponsoringFutureReserves.rawValue
    case .endSponsoringFutureReservesResult: return OperationType.endSponsoringFutureReserves.rawValue
    case .revokeSponsorshipResult: return OperationType.revokeSponsorship.rawValue
    case .clawbackResult: return OperationType.clawback.rawValue
    case .clawbackClaimableBalanceResult: return OperationType.clawbackClaimableBalance.rawValue
    case .setTrustLineFlagsResult: return OperationType.setTrustLineFlags.rawValue
    case .liquidityPoolDepositResult: return OperationType.liquidityPoolDeposit.rawValue
    case .liquidityPoolWithdrawResult: return OperationType.liquidityPoolWithdraw.rawValue
    case .invokeHostFunctionResult: return OperationType.invokeHostFunction.rawValue
    case .extendFootprintTTLResult: return OperationType.extendFootprintTTL.rawValue
    case .restoreFootprintResult: return OperationType.restoreFootprint.rawValue
    }
  }

  public func encode(to encoder: Encoder) throws {
    var container = encoder.unkeyedContainer()
    try container.encode(type())

    switch self {
    case .createAccountResult(let val):
      try container.encode(val)
    case .paymentResult(let val):
      try container.encode(val)
    case .pathPaymentStrictReceiveResult(let val):
      try container.encode(val)
    case .manageSellOfferResult(let val):
      try container.encode(val)
    case .createPassiveSellOfferResult(let val):
      try container.encode(val)
    case .setOptionsResult(let val):
      try container.encode(val)
    case .changeTrustResult(let val):
      try container.encode(val)
    case .allowTrustResult(let val):
      try container.encode(val)
    case .accountMergeResult(let val):
      try container.encode(val)
    case .inflationResult(let val):
      try container.encode(val)
    case .manageDataResult(let val):
      try container.encode(val)
    case .bumpSeqResult(let val):
      try container.encode(val)
    case .manageBuyOfferResult(let val):
      try container.encode(val)
    case .pathPaymentStrictSendResult(let val):
      try container.encode(val)
    case .createClaimableBalanceResult(let val):
      try container.encode(val)
    case .claimClaimableBalanceResult(let val):
      try container.encode(val)
    case .beginSponsoringFutureReservesResult(let val):
      try container.encode(val)
    case .endSponsoringFutureReservesResult(let val):
      try container.encode(val)
    case .revokeSponsorshipResult(let val):
      try container.encode(val)
    case .clawbackResult(let val):
      try container.encode(val)
    case .clawbackClaimableBalanceResult(let val):
      try container.encode(val)
    case .setTrustLineFlagsResult(let val):
      try container.encode(val)
    case .liquidityPoolDepositResult(let val):
      try container.encode(val)
    case .liquidityPoolWithdrawResult(let val):
      try container.encode(val)
    case .invokeHostFunctionResult(let val):
      try container.encode(val)
    case .extendFootprintTTLResult(let val):
      try container.encode(val)
    case .restoreFootprintResult(let val):
      try container.encode(val)
    }
  }
}
