// This file was automatically generated by xdrgen.
// DO NOT EDIT or your changes may be overwritten.

import Foundation

public indirect enum SCSpecTypeDefXDR: XDRCodable, Sendable {
  case val
  case bool
  case void
  case error
  case u32
  case i32
  case u64
  case i64
  case timepoint
  case duration
  case u128
  case i128
  case u256
  case i256
  case bytes
  case string
  case symbol
  case address
  case muxedAddress
  case option(SCSpecTypeOptionXDR)
  case result(SCSpecTypeResultXDR)
  case vec(SCSpecTypeVecXDR)
  case map(SCSpecTypeMapXDR)
  case tuple(SCSpecTypeTupleXDR)
  case bytesN(SCSpecTypeBytesNXDR)
  case udt(SCSpecTypeUDTXDR)

  public init(from decoder: Decoder) throws {
    var container = try decoder.unkeyedContainer()
    let discriminant = try container.decode(Int32.self)

    switch discriminant {
    case SCSpecType.val.rawValue:
      self = .val
    case SCSpecType.bool.rawValue:
      self = .bool
    case SCSpecType.void.rawValue:
      self = .void
    case SCSpecType.error.rawValue:
      self = .error
    case SCSpecType.u32.rawValue:
      self = .u32
    case SCSpecType.i32.rawValue:
      self = .i32
    case SCSpecType.u64.rawValue:
      self = .u64
    case SCSpecType.i64.rawValue:
      self = .i64
    case SCSpecType.timepoint.rawValue:
      self = .timepoint
    case SCSpecType.duration.rawValue:
      self = .duration
    case SCSpecType.u128.rawValue:
      self = .u128
    case SCSpecType.i128.rawValue:
      self = .i128
    case SCSpecType.u256.rawValue:
      self = .u256
    case SCSpecType.i256.rawValue:
      self = .i256
    case SCSpecType.bytes.rawValue:
      self = .bytes
    case SCSpecType.string.rawValue:
      self = .string
    case SCSpecType.symbol.rawValue:
      self = .symbol
    case SCSpecType.address.rawValue:
      self = .address
    case SCSpecType.muxedAddress.rawValue:
      self = .muxedAddress
    case SCSpecType.option.rawValue:
      let val = try container.decode(SCSpecTypeOptionXDR.self)
      self = .option(val)
    case SCSpecType.result.rawValue:
      let val = try container.decode(SCSpecTypeResultXDR.self)
      self = .result(val)
    case SCSpecType.vec.rawValue:
      let val = try container.decode(SCSpecTypeVecXDR.self)
      self = .vec(val)
    case SCSpecType.map.rawValue:
      let val = try container.decode(SCSpecTypeMapXDR.self)
      self = .map(val)
    case SCSpecType.tuple.rawValue:
      let val = try container.decode(SCSpecTypeTupleXDR.self)
      self = .tuple(val)
    case SCSpecType.bytesN.rawValue:
      let val = try container.decode(SCSpecTypeBytesNXDR.self)
      self = .bytesN(val)
    case SCSpecType.udt.rawValue:
      let val = try container.decode(SCSpecTypeUDTXDR.self)
      self = .udt(val)
    default:
      throw StellarSDKError.xdrDecodingError(message: "Unknown SCSpecTypeDefXDR discriminant: \(discriminant)")
    }
  }

  public func type() -> Int32 {
    switch self {
    case .val: return SCSpecType.val.rawValue
    case .bool: return SCSpecType.bool.rawValue
    case .void: return SCSpecType.void.rawValue
    case .error: return SCSpecType.error.rawValue
    case .u32: return SCSpecType.u32.rawValue
    case .i32: return SCSpecType.i32.rawValue
    case .u64: return SCSpecType.u64.rawValue
    case .i64: return SCSpecType.i64.rawValue
    case .timepoint: return SCSpecType.timepoint.rawValue
    case .duration: return SCSpecType.duration.rawValue
    case .u128: return SCSpecType.u128.rawValue
    case .i128: return SCSpecType.i128.rawValue
    case .u256: return SCSpecType.u256.rawValue
    case .i256: return SCSpecType.i256.rawValue
    case .bytes: return SCSpecType.bytes.rawValue
    case .string: return SCSpecType.string.rawValue
    case .symbol: return SCSpecType.symbol.rawValue
    case .address: return SCSpecType.address.rawValue
    case .muxedAddress: return SCSpecType.muxedAddress.rawValue
    case .option: return SCSpecType.option.rawValue
    case .result: return SCSpecType.result.rawValue
    case .vec: return SCSpecType.vec.rawValue
    case .map: return SCSpecType.map.rawValue
    case .tuple: return SCSpecType.tuple.rawValue
    case .bytesN: return SCSpecType.bytesN.rawValue
    case .udt: return SCSpecType.udt.rawValue
    }
  }

  public func encode(to encoder: Encoder) throws {
    var container = encoder.unkeyedContainer()
    try container.encode(type())

    switch self {
    case .val:
      break
    case .bool:
      break
    case .void:
      break
    case .error:
      break
    case .u32:
      break
    case .i32:
      break
    case .u64:
      break
    case .i64:
      break
    case .timepoint:
      break
    case .duration:
      break
    case .u128:
      break
    case .i128:
      break
    case .u256:
      break
    case .i256:
      break
    case .bytes:
      break
    case .string:
      break
    case .symbol:
      break
    case .address:
      break
    case .muxedAddress:
      break
    case .option(let val):
      try container.encode(val)
    case .result(let val):
      try container.encode(val)
    case .vec(let val):
      try container.encode(val)
    case .map(let val):
      try container.encode(val)
    case .tuple(let val):
      try container.encode(val)
    case .bytesN(let val):
      try container.encode(val)
    case .udt(let val):
      try container.encode(val)
    }
  }
}
