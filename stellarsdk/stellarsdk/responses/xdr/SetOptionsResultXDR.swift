// This file was automatically generated by xdrgen.
// DO NOT EDIT or your changes may be overwritten.

import Foundation

public enum SetOptionsResultXDR: XDRCodable, Sendable {
  case success
  case lowReserve
  case tooManySigners
  case badFlags
  case invalidInflation
  case cantChange
  case unknownFlag
  case thresholdOutOfRange
  case badSigner
  case invalidHomeDomain
  case authRevocableRequired

  public init(from decoder: Decoder) throws {
    var container = try decoder.unkeyedContainer()
    let discriminant = try container.decode(Int32.self)

    switch discriminant {
    case SetOptionsResultCode.success.rawValue:
      self = .success
    case SetOptionsResultCode.lowReserve.rawValue:
      self = .lowReserve
    case SetOptionsResultCode.tooManySigners.rawValue:
      self = .tooManySigners
    case SetOptionsResultCode.badFlags.rawValue:
      self = .badFlags
    case SetOptionsResultCode.invalidInflation.rawValue:
      self = .invalidInflation
    case SetOptionsResultCode.cantChange.rawValue:
      self = .cantChange
    case SetOptionsResultCode.unknownFlag.rawValue:
      self = .unknownFlag
    case SetOptionsResultCode.thresholdOutOfRange.rawValue:
      self = .thresholdOutOfRange
    case SetOptionsResultCode.badSigner.rawValue:
      self = .badSigner
    case SetOptionsResultCode.invalidHomeDomain.rawValue:
      self = .invalidHomeDomain
    case SetOptionsResultCode.authRevocableRequired.rawValue:
      self = .authRevocableRequired
    default:
      throw StellarSDKError.xdrDecodingError(message: "Unknown SetOptionsResultXDR discriminant: \(discriminant)")
    }
  }

  public func type() -> Int32 {
    switch self {
    case .success: return SetOptionsResultCode.success.rawValue
    case .lowReserve: return SetOptionsResultCode.lowReserve.rawValue
    case .tooManySigners: return SetOptionsResultCode.tooManySigners.rawValue
    case .badFlags: return SetOptionsResultCode.badFlags.rawValue
    case .invalidInflation: return SetOptionsResultCode.invalidInflation.rawValue
    case .cantChange: return SetOptionsResultCode.cantChange.rawValue
    case .unknownFlag: return SetOptionsResultCode.unknownFlag.rawValue
    case .thresholdOutOfRange: return SetOptionsResultCode.thresholdOutOfRange.rawValue
    case .badSigner: return SetOptionsResultCode.badSigner.rawValue
    case .invalidHomeDomain: return SetOptionsResultCode.invalidHomeDomain.rawValue
    case .authRevocableRequired: return SetOptionsResultCode.authRevocableRequired.rawValue
    }
  }

  public func encode(to encoder: Encoder) throws {
    var container = encoder.unkeyedContainer()
    try container.encode(type())

    switch self {
    case .success:
      break
    case .lowReserve:
      break
    case .tooManySigners:
      break
    case .badFlags:
      break
    case .invalidInflation:
      break
    case .cantChange:
      break
    case .unknownFlag:
      break
    case .thresholdOutOfRange:
      break
    case .badSigner:
      break
    case .invalidHomeDomain:
      break
    case .authRevocableRequired:
      break
    }
  }
}

extension SetOptionsResultXDR: XdrJsonCodable {
  public func toXdrJsonValue() throws -> XdrJsonValue {
    switch self {
    case .success: return .string("success")
    case .lowReserve: return .string("low_reserve")
    case .tooManySigners: return .string("too_many_signers")
    case .badFlags: return .string("bad_flags")
    case .invalidInflation: return .string("invalid_inflation")
    case .cantChange: return .string("cant_change")
    case .unknownFlag: return .string("unknown_flag")
    case .thresholdOutOfRange: return .string("threshold_out_of_range")
    case .badSigner: return .string("bad_signer")
    case .invalidHomeDomain: return .string("invalid_home_domain")
    case .authRevocableRequired: return .string("auth_revocable_required")
    }
  }

  public static func fromXdrJsonValue(_ value: XdrJsonValue) throws -> SetOptionsResultXDR {
    let name = try XdrJson.string(value, type: "SetOptionsResultXDR")
    switch name {
    case "success":
      return .success
    case "low_reserve":
      return .lowReserve
    case "too_many_signers":
      return .tooManySigners
    case "bad_flags":
      return .badFlags
    case "invalid_inflation":
      return .invalidInflation
    case "cant_change":
      return .cantChange
    case "unknown_flag":
      return .unknownFlag
    case "threshold_out_of_range":
      return .thresholdOutOfRange
    case "bad_signer":
      return .badSigner
    case "invalid_home_domain":
      return .invalidHomeDomain
    case "auth_revocable_required":
      return .authRevocableRequired
    default:
      throw XdrJsonError.unknownUnionArm(type: "SetOptionsResultXDR", key: name)
    }
  }
}
