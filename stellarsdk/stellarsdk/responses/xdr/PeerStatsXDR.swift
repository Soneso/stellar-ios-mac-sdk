// This file was automatically generated by xdrgen.
// DO NOT EDIT or your changes may be overwritten.

import Foundation

public struct PeerStatsXDR: XDRCodable, Sendable {
  public var id: NodeIDXDR
  public var versionStr: String
  public var messagesRead: UInt64
  public var messagesWritten: UInt64
  public var bytesRead: UInt64
  public var bytesWritten: UInt64
  public var secondsConnected: UInt64
  public var uniqueFloodBytesRecv: UInt64
  public var duplicateFloodBytesRecv: UInt64
  public var uniqueFetchBytesRecv: UInt64
  public var duplicateFetchBytesRecv: UInt64
  public var uniqueFloodMessageRecv: UInt64
  public var duplicateFloodMessageRecv: UInt64
  public var uniqueFetchMessageRecv: UInt64
  public var duplicateFetchMessageRecv: UInt64

  public init(
    id: NodeIDXDR,
    versionStr: String,
    messagesRead: UInt64,
    messagesWritten: UInt64,
    bytesRead: UInt64,
    bytesWritten: UInt64,
    secondsConnected: UInt64,
    uniqueFloodBytesRecv: UInt64,
    duplicateFloodBytesRecv: UInt64,
    uniqueFetchBytesRecv: UInt64,
    duplicateFetchBytesRecv: UInt64,
    uniqueFloodMessageRecv: UInt64,
    duplicateFloodMessageRecv: UInt64,
    uniqueFetchMessageRecv: UInt64,
    duplicateFetchMessageRecv: UInt64
  ) {
    self.id = id
    self.versionStr = versionStr
    self.messagesRead = messagesRead
    self.messagesWritten = messagesWritten
    self.bytesRead = bytesRead
    self.bytesWritten = bytesWritten
    self.secondsConnected = secondsConnected
    self.uniqueFloodBytesRecv = uniqueFloodBytesRecv
    self.duplicateFloodBytesRecv = duplicateFloodBytesRecv
    self.uniqueFetchBytesRecv = uniqueFetchBytesRecv
    self.duplicateFetchBytesRecv = duplicateFetchBytesRecv
    self.uniqueFloodMessageRecv = uniqueFloodMessageRecv
    self.duplicateFloodMessageRecv = duplicateFloodMessageRecv
    self.uniqueFetchMessageRecv = uniqueFetchMessageRecv
    self.duplicateFetchMessageRecv = duplicateFetchMessageRecv
  }

  public init(from decoder: Decoder) throws {
    var container = try decoder.unkeyedContainer()
    id = try container.decode(NodeIDXDR.self)
    versionStr = try container.decode(String.self)
    messagesRead = try container.decode(UInt64.self)
    messagesWritten = try container.decode(UInt64.self)
    bytesRead = try container.decode(UInt64.self)
    bytesWritten = try container.decode(UInt64.self)
    secondsConnected = try container.decode(UInt64.self)
    uniqueFloodBytesRecv = try container.decode(UInt64.self)
    duplicateFloodBytesRecv = try container.decode(UInt64.self)
    uniqueFetchBytesRecv = try container.decode(UInt64.self)
    duplicateFetchBytesRecv = try container.decode(UInt64.self)
    uniqueFloodMessageRecv = try container.decode(UInt64.self)
    duplicateFloodMessageRecv = try container.decode(UInt64.self)
    uniqueFetchMessageRecv = try container.decode(UInt64.self)
    duplicateFetchMessageRecv = try container.decode(UInt64.self)
  }

  public func encode(to encoder: Encoder) throws {
    var container = encoder.unkeyedContainer()
    try container.encode(id)
    try container.encode(versionStr)
    try container.encode(messagesRead)
    try container.encode(messagesWritten)
    try container.encode(bytesRead)
    try container.encode(bytesWritten)
    try container.encode(secondsConnected)
    try container.encode(uniqueFloodBytesRecv)
    try container.encode(duplicateFloodBytesRecv)
    try container.encode(uniqueFetchBytesRecv)
    try container.encode(duplicateFetchBytesRecv)
    try container.encode(uniqueFloodMessageRecv)
    try container.encode(duplicateFloodMessageRecv)
    try container.encode(uniqueFetchMessageRecv)
    try container.encode(duplicateFetchMessageRecv)
  }
}
