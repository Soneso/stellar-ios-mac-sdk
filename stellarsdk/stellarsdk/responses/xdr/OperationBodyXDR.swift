// This file was automatically generated by xdrgen.
// DO NOT EDIT or your changes may be overwritten.

import Foundation

public enum OperationBodyXDR: XDRCodable, Sendable {
  case createAccountOp(CreateAccountOperationXDR)
  case paymentOp(PaymentOperationXDR)
  case pathPaymentStrictReceiveOp(PathPaymentOperationXDR)
  case manageSellOfferOp(ManageOfferOperationXDR)
  case createPassiveSellOfferOp(CreatePassiveOfferOperationXDR)
  case setOptionsOp(SetOptionsOperationXDR)
  case changeTrustOp(ChangeTrustOperationXDR)
  case allowTrustOp(AllowTrustOperationXDR)
  case accountMerge(MuxedAccountXDR)
  case inflation
  case manageDataOp(ManageDataOperationXDR)
  case bumpSequenceOp(BumpSequenceOperationXDR)
  case manageBuyOfferOp(ManageOfferOperationXDR)
  case pathPaymentStrictSendOp(PathPaymentOperationXDR)
  case createClaimableBalanceOp(CreateClaimableBalanceOpXDR)
  case claimClaimableBalanceOp(ClaimClaimableBalanceOpXDR)
  case beginSponsoringFutureReservesOp(BeginSponsoringFutureReservesOpXDR)
  case endSponsoringFutureReserves
  case revokeSponsorshipOp(RevokeSponsorshipOpXDR)
  case clawbackOp(ClawbackOpXDR)
  case clawbackClaimableBalanceOp(ClawbackClaimableBalanceOpXDR)
  case setTrustLineFlagsOp(SetTrustLineFlagsOpXDR)
  case liquidityPoolDepositOp(LiquidityPoolDepositOpXDR)
  case liquidityPoolWithdrawOp(LiquidityPoolWithdrawOpXDR)
  case invokeHostFunctionOp(InvokeHostFunctionOpXDR)
  case extendFootprintTTLOp(ExtendFootprintTTLOpXDR)
  case restoreFootprintOp(RestoreFootprintOpXDR)

  public init(from decoder: Decoder) throws {
    var container = try decoder.unkeyedContainer()
    let discriminant = try container.decode(Int32.self)

    switch discriminant {
    case OperationType.accountCreated.rawValue:
      let val = try container.decode(CreateAccountOperationXDR.self)
      self = .createAccountOp(val)
    case OperationType.payment.rawValue:
      let val = try container.decode(PaymentOperationXDR.self)
      self = .paymentOp(val)
    case OperationType.pathPayment.rawValue:
      let val = try container.decode(PathPaymentOperationXDR.self)
      self = .pathPaymentStrictReceiveOp(val)
    case OperationType.manageSellOffer.rawValue:
      let val = try container.decode(ManageOfferOperationXDR.self)
      self = .manageSellOfferOp(val)
    case OperationType.createPassiveSellOffer.rawValue:
      let val = try container.decode(CreatePassiveOfferOperationXDR.self)
      self = .createPassiveSellOfferOp(val)
    case OperationType.setOptions.rawValue:
      let val = try container.decode(SetOptionsOperationXDR.self)
      self = .setOptionsOp(val)
    case OperationType.changeTrust.rawValue:
      let val = try container.decode(ChangeTrustOperationXDR.self)
      self = .changeTrustOp(val)
    case OperationType.allowTrust.rawValue:
      let val = try container.decode(AllowTrustOperationXDR.self)
      self = .allowTrustOp(val)
    case OperationType.accountMerge.rawValue:
      let val = try container.decode(MuxedAccountXDR.self)
      self = .accountMerge(val)
    case OperationType.inflation.rawValue:
      self = .inflation
    case OperationType.manageData.rawValue:
      let val = try container.decode(ManageDataOperationXDR.self)
      self = .manageDataOp(val)
    case OperationType.bumpSequence.rawValue:
      let val = try container.decode(BumpSequenceOperationXDR.self)
      self = .bumpSequenceOp(val)
    case OperationType.manageBuyOffer.rawValue:
      let val = try container.decode(ManageOfferOperationXDR.self)
      self = .manageBuyOfferOp(val)
    case OperationType.pathPaymentStrictSend.rawValue:
      let val = try container.decode(PathPaymentOperationXDR.self)
      self = .pathPaymentStrictSendOp(val)
    case OperationType.createClaimableBalance.rawValue:
      let val = try container.decode(CreateClaimableBalanceOpXDR.self)
      self = .createClaimableBalanceOp(val)
    case OperationType.claimClaimableBalance.rawValue:
      let val = try container.decode(ClaimClaimableBalanceOpXDR.self)
      self = .claimClaimableBalanceOp(val)
    case OperationType.beginSponsoringFutureReserves.rawValue:
      let val = try container.decode(BeginSponsoringFutureReservesOpXDR.self)
      self = .beginSponsoringFutureReservesOp(val)
    case OperationType.endSponsoringFutureReserves.rawValue:
      self = .endSponsoringFutureReserves
    case OperationType.revokeSponsorship.rawValue:
      let val = try container.decode(RevokeSponsorshipOpXDR.self)
      self = .revokeSponsorshipOp(val)
    case OperationType.clawback.rawValue:
      let val = try container.decode(ClawbackOpXDR.self)
      self = .clawbackOp(val)
    case OperationType.clawbackClaimableBalance.rawValue:
      let val = try container.decode(ClawbackClaimableBalanceOpXDR.self)
      self = .clawbackClaimableBalanceOp(val)
    case OperationType.setTrustLineFlags.rawValue:
      let val = try container.decode(SetTrustLineFlagsOpXDR.self)
      self = .setTrustLineFlagsOp(val)
    case OperationType.liquidityPoolDeposit.rawValue:
      let val = try container.decode(LiquidityPoolDepositOpXDR.self)
      self = .liquidityPoolDepositOp(val)
    case OperationType.liquidityPoolWithdraw.rawValue:
      let val = try container.decode(LiquidityPoolWithdrawOpXDR.self)
      self = .liquidityPoolWithdrawOp(val)
    case OperationType.invokeHostFunction.rawValue:
      let val = try container.decode(InvokeHostFunctionOpXDR.self)
      self = .invokeHostFunctionOp(val)
    case OperationType.extendFootprintTTL.rawValue:
      let val = try container.decode(ExtendFootprintTTLOpXDR.self)
      self = .extendFootprintTTLOp(val)
    case OperationType.restoreFootprint.rawValue:
      let val = try container.decode(RestoreFootprintOpXDR.self)
      self = .restoreFootprintOp(val)
    default:
      throw StellarSDKError.xdrDecodingError(message: "Unknown OperationBodyXDR discriminant: \(discriminant)")
    }
  }

  public func type() -> Int32 {
    switch self {
    case .createAccountOp: return OperationType.accountCreated.rawValue
    case .paymentOp: return OperationType.payment.rawValue
    case .pathPaymentStrictReceiveOp: return OperationType.pathPayment.rawValue
    case .manageSellOfferOp: return OperationType.manageSellOffer.rawValue
    case .createPassiveSellOfferOp: return OperationType.createPassiveSellOffer.rawValue
    case .setOptionsOp: return OperationType.setOptions.rawValue
    case .changeTrustOp: return OperationType.changeTrust.rawValue
    case .allowTrustOp: return OperationType.allowTrust.rawValue
    case .accountMerge: return OperationType.accountMerge.rawValue
    case .inflation: return OperationType.inflation.rawValue
    case .manageDataOp: return OperationType.manageData.rawValue
    case .bumpSequenceOp: return OperationType.bumpSequence.rawValue
    case .manageBuyOfferOp: return OperationType.manageBuyOffer.rawValue
    case .pathPaymentStrictSendOp: return OperationType.pathPaymentStrictSend.rawValue
    case .createClaimableBalanceOp: return OperationType.createClaimableBalance.rawValue
    case .claimClaimableBalanceOp: return OperationType.claimClaimableBalance.rawValue
    case .beginSponsoringFutureReservesOp: return OperationType.beginSponsoringFutureReserves.rawValue
    case .endSponsoringFutureReserves: return OperationType.endSponsoringFutureReserves.rawValue
    case .revokeSponsorshipOp: return OperationType.revokeSponsorship.rawValue
    case .clawbackOp: return OperationType.clawback.rawValue
    case .clawbackClaimableBalanceOp: return OperationType.clawbackClaimableBalance.rawValue
    case .setTrustLineFlagsOp: return OperationType.setTrustLineFlags.rawValue
    case .liquidityPoolDepositOp: return OperationType.liquidityPoolDeposit.rawValue
    case .liquidityPoolWithdrawOp: return OperationType.liquidityPoolWithdraw.rawValue
    case .invokeHostFunctionOp: return OperationType.invokeHostFunction.rawValue
    case .extendFootprintTTLOp: return OperationType.extendFootprintTTL.rawValue
    case .restoreFootprintOp: return OperationType.restoreFootprint.rawValue
    }
  }

  public func encode(to encoder: Encoder) throws {
    var container = encoder.unkeyedContainer()
    try container.encode(type())

    switch self {
    case .createAccountOp(let val):
      try container.encode(val)
    case .paymentOp(let val):
      try container.encode(val)
    case .pathPaymentStrictReceiveOp(let val):
      try container.encode(val)
    case .manageSellOfferOp(let val):
      try container.encode(val)
    case .createPassiveSellOfferOp(let val):
      try container.encode(val)
    case .setOptionsOp(let val):
      try container.encode(val)
    case .changeTrustOp(let val):
      try container.encode(val)
    case .allowTrustOp(let val):
      try container.encode(val)
    case .accountMerge(let val):
      try container.encode(val)
    case .inflation:
      break
    case .manageDataOp(let val):
      try container.encode(val)
    case .bumpSequenceOp(let val):
      try container.encode(val)
    case .manageBuyOfferOp(let val):
      try container.encode(val)
    case .pathPaymentStrictSendOp(let val):
      try container.encode(val)
    case .createClaimableBalanceOp(let val):
      try container.encode(val)
    case .claimClaimableBalanceOp(let val):
      try container.encode(val)
    case .beginSponsoringFutureReservesOp(let val):
      try container.encode(val)
    case .endSponsoringFutureReserves:
      break
    case .revokeSponsorshipOp(let val):
      try container.encode(val)
    case .clawbackOp(let val):
      try container.encode(val)
    case .clawbackClaimableBalanceOp(let val):
      try container.encode(val)
    case .setTrustLineFlagsOp(let val):
      try container.encode(val)
    case .liquidityPoolDepositOp(let val):
      try container.encode(val)
    case .liquidityPoolWithdrawOp(let val):
      try container.encode(val)
    case .invokeHostFunctionOp(let val):
      try container.encode(val)
    case .extendFootprintTTLOp(let val):
      try container.encode(val)
    case .restoreFootprintOp(let val):
      try container.encode(val)
    }
  }
}
