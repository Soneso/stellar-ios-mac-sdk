// This file was automatically generated by xdrgen.
// DO NOT EDIT or your changes may be overwritten.

import Foundation

public enum OperationBodyXDR: XDRCodable, Sendable {
  case createAccountOp(CreateAccountOperationXDR)
  case paymentOp(PaymentOperationXDR)
  case pathPaymentStrictReceiveOp(PathPaymentOperationXDR)
  case manageSellOfferOp(ManageOfferOperationXDR)
  case createPassiveSellOfferOp(CreatePassiveOfferOperationXDR)
  case setOptionsOp(SetOptionsOperationXDR)
  case changeTrustOp(ChangeTrustOperationXDR)
  case allowTrustOp(AllowTrustOperationXDR)
  case accountMerge(MuxedAccountXDR)
  case inflation
  case manageDataOp(ManageDataOperationXDR)
  case bumpSequenceOp(BumpSequenceOperationXDR)
  case manageBuyOfferOp(ManageOfferOperationXDR)
  case pathPaymentStrictSendOp(PathPaymentOperationXDR)
  case createClaimableBalanceOp(CreateClaimableBalanceOpXDR)
  case claimClaimableBalanceOp(ClaimClaimableBalanceOpXDR)
  case beginSponsoringFutureReservesOp(BeginSponsoringFutureReservesOpXDR)
  case endSponsoringFutureReserves
  case revokeSponsorshipOp(RevokeSponsorshipOpXDR)
  case clawbackOp(ClawbackOpXDR)
  case clawbackClaimableBalanceOp(ClawbackClaimableBalanceOpXDR)
  case setTrustLineFlagsOp(SetTrustLineFlagsOpXDR)
  case liquidityPoolDepositOp(LiquidityPoolDepositOpXDR)
  case liquidityPoolWithdrawOp(LiquidityPoolWithdrawOpXDR)
  case invokeHostFunctionOp(InvokeHostFunctionOpXDR)
  case extendFootprintTTLOp(ExtendFootprintTTLOpXDR)
  case restoreFootprintOp(RestoreFootprintOpXDR)

  public init(from decoder: Decoder) throws {
    var container = try decoder.unkeyedContainer()
    let discriminant = try container.decode(Int32.self)

    switch discriminant {
    case OperationType.accountCreated.rawValue:
      let val = try container.decode(CreateAccountOperationXDR.self)
      self = .createAccountOp(val)
    case OperationType.payment.rawValue:
      let val = try container.decode(PaymentOperationXDR.self)
      self = .paymentOp(val)
    case OperationType.pathPayment.rawValue:
      let val = try container.decode(PathPaymentOperationXDR.self)
      self = .pathPaymentStrictReceiveOp(val)
    case OperationType.manageSellOffer.rawValue:
      let val = try container.decode(ManageOfferOperationXDR.self)
      self = .manageSellOfferOp(val)
    case OperationType.createPassiveSellOffer.rawValue:
      let val = try container.decode(CreatePassiveOfferOperationXDR.self)
      self = .createPassiveSellOfferOp(val)
    case OperationType.setOptions.rawValue:
      let val = try container.decode(SetOptionsOperationXDR.self)
      self = .setOptionsOp(val)
    case OperationType.changeTrust.rawValue:
      let val = try container.decode(ChangeTrustOperationXDR.self)
      self = .changeTrustOp(val)
    case OperationType.allowTrust.rawValue:
      let val = try container.decode(AllowTrustOperationXDR.self)
      self = .allowTrustOp(val)
    case OperationType.accountMerge.rawValue:
      let val = try container.decode(MuxedAccountXDR.self)
      self = .accountMerge(val)
    case OperationType.inflation.rawValue:
      self = .inflation
    case OperationType.manageData.rawValue:
      let val = try container.decode(ManageDataOperationXDR.self)
      self = .manageDataOp(val)
    case OperationType.bumpSequence.rawValue:
      let val = try container.decode(BumpSequenceOperationXDR.self)
      self = .bumpSequenceOp(val)
    case OperationType.manageBuyOffer.rawValue:
      let val = try container.decode(ManageOfferOperationXDR.self)
      self = .manageBuyOfferOp(val)
    case OperationType.pathPaymentStrictSend.rawValue:
      let val = try container.decode(PathPaymentOperationXDR.self)
      self = .pathPaymentStrictSendOp(val)
    case OperationType.createClaimableBalance.rawValue:
      let val = try container.decode(CreateClaimableBalanceOpXDR.self)
      self = .createClaimableBalanceOp(val)
    case OperationType.claimClaimableBalance.rawValue:
      let val = try container.decode(ClaimClaimableBalanceOpXDR.self)
      self = .claimClaimableBalanceOp(val)
    case OperationType.beginSponsoringFutureReserves.rawValue:
      let val = try container.decode(BeginSponsoringFutureReservesOpXDR.self)
      self = .beginSponsoringFutureReservesOp(val)
    case OperationType.endSponsoringFutureReserves.rawValue:
      self = .endSponsoringFutureReserves
    case OperationType.revokeSponsorship.rawValue:
      let val = try container.decode(RevokeSponsorshipOpXDR.self)
      self = .revokeSponsorshipOp(val)
    case OperationType.clawback.rawValue:
      let val = try container.decode(ClawbackOpXDR.self)
      self = .clawbackOp(val)
    case OperationType.clawbackClaimableBalance.rawValue:
      let val = try container.decode(ClawbackClaimableBalanceOpXDR.self)
      self = .clawbackClaimableBalanceOp(val)
    case OperationType.setTrustLineFlags.rawValue:
      let val = try container.decode(SetTrustLineFlagsOpXDR.self)
      self = .setTrustLineFlagsOp(val)
    case OperationType.liquidityPoolDeposit.rawValue:
      let val = try container.decode(LiquidityPoolDepositOpXDR.self)
      self = .liquidityPoolDepositOp(val)
    case OperationType.liquidityPoolWithdraw.rawValue:
      let val = try container.decode(LiquidityPoolWithdrawOpXDR.self)
      self = .liquidityPoolWithdrawOp(val)
    case OperationType.invokeHostFunction.rawValue:
      let val = try container.decode(InvokeHostFunctionOpXDR.self)
      self = .invokeHostFunctionOp(val)
    case OperationType.extendFootprintTTL.rawValue:
      let val = try container.decode(ExtendFootprintTTLOpXDR.self)
      self = .extendFootprintTTLOp(val)
    case OperationType.restoreFootprint.rawValue:
      let val = try container.decode(RestoreFootprintOpXDR.self)
      self = .restoreFootprintOp(val)
    default:
      throw StellarSDKError.xdrDecodingError(message: "Unknown OperationBodyXDR discriminant: \(discriminant)")
    }
  }

  public func type() -> Int32 {
    switch self {
    case .createAccountOp: return OperationType.accountCreated.rawValue
    case .paymentOp: return OperationType.payment.rawValue
    case .pathPaymentStrictReceiveOp: return OperationType.pathPayment.rawValue
    case .manageSellOfferOp: return OperationType.manageSellOffer.rawValue
    case .createPassiveSellOfferOp: return OperationType.createPassiveSellOffer.rawValue
    case .setOptionsOp: return OperationType.setOptions.rawValue
    case .changeTrustOp: return OperationType.changeTrust.rawValue
    case .allowTrustOp: return OperationType.allowTrust.rawValue
    case .accountMerge: return OperationType.accountMerge.rawValue
    case .inflation: return OperationType.inflation.rawValue
    case .manageDataOp: return OperationType.manageData.rawValue
    case .bumpSequenceOp: return OperationType.bumpSequence.rawValue
    case .manageBuyOfferOp: return OperationType.manageBuyOffer.rawValue
    case .pathPaymentStrictSendOp: return OperationType.pathPaymentStrictSend.rawValue
    case .createClaimableBalanceOp: return OperationType.createClaimableBalance.rawValue
    case .claimClaimableBalanceOp: return OperationType.claimClaimableBalance.rawValue
    case .beginSponsoringFutureReservesOp: return OperationType.beginSponsoringFutureReserves.rawValue
    case .endSponsoringFutureReserves: return OperationType.endSponsoringFutureReserves.rawValue
    case .revokeSponsorshipOp: return OperationType.revokeSponsorship.rawValue
    case .clawbackOp: return OperationType.clawback.rawValue
    case .clawbackClaimableBalanceOp: return OperationType.clawbackClaimableBalance.rawValue
    case .setTrustLineFlagsOp: return OperationType.setTrustLineFlags.rawValue
    case .liquidityPoolDepositOp: return OperationType.liquidityPoolDeposit.rawValue
    case .liquidityPoolWithdrawOp: return OperationType.liquidityPoolWithdraw.rawValue
    case .invokeHostFunctionOp: return OperationType.invokeHostFunction.rawValue
    case .extendFootprintTTLOp: return OperationType.extendFootprintTTL.rawValue
    case .restoreFootprintOp: return OperationType.restoreFootprint.rawValue
    }
  }

  public func encode(to encoder: Encoder) throws {
    var container = encoder.unkeyedContainer()
    try container.encode(type())

    switch self {
    case .createAccountOp(let val):
      try container.encode(val)
    case .paymentOp(let val):
      try container.encode(val)
    case .pathPaymentStrictReceiveOp(let val):
      try container.encode(val)
    case .manageSellOfferOp(let val):
      try container.encode(val)
    case .createPassiveSellOfferOp(let val):
      try container.encode(val)
    case .setOptionsOp(let val):
      try container.encode(val)
    case .changeTrustOp(let val):
      try container.encode(val)
    case .allowTrustOp(let val):
      try container.encode(val)
    case .accountMerge(let val):
      try container.encode(val)
    case .inflation:
      break
    case .manageDataOp(let val):
      try container.encode(val)
    case .bumpSequenceOp(let val):
      try container.encode(val)
    case .manageBuyOfferOp(let val):
      try container.encode(val)
    case .pathPaymentStrictSendOp(let val):
      try container.encode(val)
    case .createClaimableBalanceOp(let val):
      try container.encode(val)
    case .claimClaimableBalanceOp(let val):
      try container.encode(val)
    case .beginSponsoringFutureReservesOp(let val):
      try container.encode(val)
    case .endSponsoringFutureReserves:
      break
    case .revokeSponsorshipOp(let val):
      try container.encode(val)
    case .clawbackOp(let val):
      try container.encode(val)
    case .clawbackClaimableBalanceOp(let val):
      try container.encode(val)
    case .setTrustLineFlagsOp(let val):
      try container.encode(val)
    case .liquidityPoolDepositOp(let val):
      try container.encode(val)
    case .liquidityPoolWithdrawOp(let val):
      try container.encode(val)
    case .invokeHostFunctionOp(let val):
      try container.encode(val)
    case .extendFootprintTTLOp(let val):
      try container.encode(val)
    case .restoreFootprintOp(let val):
      try container.encode(val)
    }
  }
}

extension OperationBodyXDR {
  public func toTxRep(prefix: String, lines: inout [String]) throws {
    switch self {
    case .createAccountOp(let val):
      lines.append("\(prefix).type: CREATE_ACCOUNT")
      try val.toTxRep(prefix: "\(prefix).createAccountOp", lines: &lines)
    case .paymentOp(let val):
      lines.append("\(prefix).type: PAYMENT")
      try val.toTxRep(prefix: "\(prefix).paymentOp", lines: &lines)
    case .pathPaymentStrictReceiveOp(let val):
      lines.append("\(prefix).type: PATH_PAYMENT_STRICT_RECEIVE")
      lines.append("\(prefix).pathPaymentStrictReceiveOp.sendAsset: \(TxRepHelper.formatAsset(val.sendAsset))")
      lines.append("\(prefix).pathPaymentStrictReceiveOp.sendMax: \(val.sendMax)")
      lines.append("\(prefix).pathPaymentStrictReceiveOp.destination: \(try TxRepHelper.formatMuxedAccount(val.destination))")
      lines.append("\(prefix).pathPaymentStrictReceiveOp.destAsset: \(TxRepHelper.formatAsset(val.destinationAsset))")
      lines.append("\(prefix).pathPaymentStrictReceiveOp.destAmount: \(val.destinationAmount)")
      lines.append("\(prefix).pathPaymentStrictReceiveOp.path.len: \(val.path.count)")
      for (i, item) in val.path.enumerated() {
        lines.append("\(prefix).pathPaymentStrictReceiveOp.path[\(i)]: \(TxRepHelper.formatAsset(item))")
      }
    case .manageSellOfferOp(let val):
      lines.append("\(prefix).type: MANAGE_SELL_OFFER")
      try val.toTxRep(prefix: "\(prefix).manageSellOfferOp", lines: &lines)
    case .createPassiveSellOfferOp(let val):
      lines.append("\(prefix).type: CREATE_PASSIVE_SELL_OFFER")
      try val.toTxRep(prefix: "\(prefix).createPassiveSellOfferOp", lines: &lines)
    case .setOptionsOp(let val):
      lines.append("\(prefix).type: SET_OPTIONS")
      try val.toTxRep(prefix: "\(prefix).setOptionsOp", lines: &lines)
    case .changeTrustOp(let val):
      lines.append("\(prefix).type: CHANGE_TRUST")
      try val.toTxRep(prefix: "\(prefix).changeTrustOp", lines: &lines)
    case .allowTrustOp(let val):
      lines.append("\(prefix).type: ALLOW_TRUST")
      try val.toTxRep(prefix: "\(prefix).allowTrustOp", lines: &lines)
    case .accountMerge(let val):
      lines.append("\(prefix).type: ACCOUNT_MERGE")
      lines.append("\(prefix).destination: \(try TxRepHelper.formatMuxedAccount(val))")
    case .inflation:
      lines.append("\(prefix).type: INFLATION")
    case .manageDataOp(let val):
      lines.append("\(prefix).type: MANAGE_DATA")
      try val.toTxRep(prefix: "\(prefix).manageDataOp", lines: &lines)
    case .bumpSequenceOp(let val):
      lines.append("\(prefix).type: BUMP_SEQUENCE")
      try val.toTxRep(prefix: "\(prefix).bumpSequenceOp", lines: &lines)
    case .manageBuyOfferOp(let val):
      lines.append("\(prefix).type: MANAGE_BUY_OFFER")
      lines.append("\(prefix).manageBuyOfferOp.selling: \(TxRepHelper.formatAsset(val.selling))")
      lines.append("\(prefix).manageBuyOfferOp.buying: \(TxRepHelper.formatAsset(val.buying))")
      lines.append("\(prefix).manageBuyOfferOp.buyAmount: \(val.amount)")
      try val.price.toTxRep(prefix: "\(prefix).manageBuyOfferOp.price", lines: &lines)
      lines.append("\(prefix).manageBuyOfferOp.offerID: \(val.offerID)")
    case .pathPaymentStrictSendOp(let val):
      lines.append("\(prefix).type: PATH_PAYMENT_STRICT_SEND")
      lines.append("\(prefix).pathPaymentStrictSendOp.sendAsset: \(TxRepHelper.formatAsset(val.sendAsset))")
      lines.append("\(prefix).pathPaymentStrictSendOp.sendAmount: \(val.sendMax)")
      lines.append("\(prefix).pathPaymentStrictSendOp.destination: \(try TxRepHelper.formatMuxedAccount(val.destination))")
      lines.append("\(prefix).pathPaymentStrictSendOp.destAsset: \(TxRepHelper.formatAsset(val.destinationAsset))")
      lines.append("\(prefix).pathPaymentStrictSendOp.destMin: \(val.destinationAmount)")
      lines.append("\(prefix).pathPaymentStrictSendOp.path.len: \(val.path.count)")
      for (i, item) in val.path.enumerated() {
        lines.append("\(prefix).pathPaymentStrictSendOp.path[\(i)]: \(TxRepHelper.formatAsset(item))")
      }
    case .createClaimableBalanceOp(let val):
      lines.append("\(prefix).type: CREATE_CLAIMABLE_BALANCE")
      try val.toTxRep(prefix: "\(prefix).createClaimableBalanceOp", lines: &lines)
    case .claimClaimableBalanceOp(let val):
      lines.append("\(prefix).type: CLAIM_CLAIMABLE_BALANCE")
      try val.toTxRep(prefix: "\(prefix).claimClaimableBalanceOp", lines: &lines)
    case .beginSponsoringFutureReservesOp(let val):
      lines.append("\(prefix).type: BEGIN_SPONSORING_FUTURE_RESERVES")
      lines.append("\(prefix).beginSponsoringFutureReservesOp.sponsoredID: \(TxRepHelper.formatAccountId(val.sponsoredId))")
    case .endSponsoringFutureReserves:
      lines.append("\(prefix).type: END_SPONSORING_FUTURE_RESERVES")
    case .revokeSponsorshipOp(let val):
      lines.append("\(prefix).type: REVOKE_SPONSORSHIP")
      try val.toTxRep(prefix: "\(prefix).revokeSponsorshipOp", lines: &lines)
    case .clawbackOp(let val):
      lines.append("\(prefix).type: CLAWBACK")
      try val.toTxRep(prefix: "\(prefix).clawbackOp", lines: &lines)
    case .clawbackClaimableBalanceOp(let val):
      lines.append("\(prefix).type: CLAWBACK_CLAIMABLE_BALANCE")
      try val.toTxRep(prefix: "\(prefix).clawbackClaimableBalanceOp", lines: &lines)
    case .setTrustLineFlagsOp(let val):
      lines.append("\(prefix).type: SET_TRUST_LINE_FLAGS")
      lines.append("\(prefix).setTrustLineFlagsOp.trustor: \(TxRepHelper.formatAccountId(val.accountID))")
      lines.append("\(prefix).setTrustLineFlagsOp.asset: \(TxRepHelper.formatAsset(val.asset))")
      lines.append("\(prefix).setTrustLineFlagsOp.clearFlags: \(val.clearFlags)")
      lines.append("\(prefix).setTrustLineFlagsOp.setFlags: \(val.setFlags)")
    case .liquidityPoolDepositOp(let val):
      lines.append("\(prefix).type: LIQUIDITY_POOL_DEPOSIT")
      try val.toTxRep(prefix: "\(prefix).liquidityPoolDepositOp", lines: &lines)
    case .liquidityPoolWithdrawOp(let val):
      lines.append("\(prefix).type: LIQUIDITY_POOL_WITHDRAW")
      try val.toTxRep(prefix: "\(prefix).liquidityPoolWithdrawOp", lines: &lines)
    case .invokeHostFunctionOp(let val):
      lines.append("\(prefix).type: INVOKE_HOST_FUNCTION")
      try val.toTxRep(prefix: "\(prefix).invokeHostFunctionOp", lines: &lines)
    case .extendFootprintTTLOp(let val):
      lines.append("\(prefix).type: EXTEND_FOOTPRINT_TTL")
      try val.toTxRep(prefix: "\(prefix).extendFootprintTTLOp", lines: &lines)
    case .restoreFootprintOp(let val):
      lines.append("\(prefix).type: RESTORE_FOOTPRINT")
      try val.toTxRep(prefix: "\(prefix).restoreFootprintOp", lines: &lines)
    }
  }

  public static func fromTxRep(_ map: [String: String], prefix: String) throws -> OperationBodyXDR {
    let discKey = "\(prefix).type"
    guard let discName = TxRepHelper.getValue(map, discKey) else {
      throw TxRepError.missingValue(key: discKey)
    }
    switch discName {
    case "CREATE_ACCOUNT":
      let val = try CreateAccountOperationXDR.fromTxRep(map, prefix: "\(prefix).createAccountOp")
      return .createAccountOp(val)
    case "PAYMENT":
      let val = try PaymentOperationXDR.fromTxRep(map, prefix: "\(prefix).paymentOp")
      return .paymentOp(val)
    case "PATH_PAYMENT_STRICT_RECEIVE":
      let sendAsset: AssetXDR = try TxRepHelper.requireAsset(map, "\(prefix).pathPaymentStrictReceiveOp.sendAsset")
      let sendMax: Int64 = try TxRepHelper.parseInt64(TxRepHelper.getValue(map, "\(prefix).pathPaymentStrictReceiveOp.sendMax") ?? "0")
      let destination: MuxedAccountXDR = try TxRepHelper.requireMuxedAccount(map, "\(prefix).pathPaymentStrictReceiveOp.destination")
      let destinationAsset: AssetXDR = try TxRepHelper.requireAsset(map, "\(prefix).pathPaymentStrictReceiveOp.destAsset")
      let destinationAmount: Int64 = try TxRepHelper.parseInt64(TxRepHelper.getValue(map, "\(prefix).pathPaymentStrictReceiveOp.destAmount") ?? "0")
      let pathLen = try TxRepHelper.parseInt(TxRepHelper.getValue(map, "\(prefix).pathPaymentStrictReceiveOp.path.len") ?? "0")
      var path = [AssetXDR]()
      for i in 0..<Int(pathLen) {
        let item: AssetXDR = try TxRepHelper.parseAsset(TxRepHelper.getValue(map, "\(prefix).pathPaymentStrictReceiveOp.path[\(i)]") ?? "")
        path.append(item)
      }
      let val = PathPaymentOperationXDR(sendAsset: sendAsset, sendMax: sendMax, destination: destination, destinationAsset: destinationAsset, destinationAmount: destinationAmount, path: path)
      return .pathPaymentStrictReceiveOp(val)
    case "MANAGE_SELL_OFFER":
      let val = try ManageOfferOperationXDR.fromTxRep(map, prefix: "\(prefix).manageSellOfferOp")
      return .manageSellOfferOp(val)
    case "CREATE_PASSIVE_SELL_OFFER":
      let val = try CreatePassiveOfferOperationXDR.fromTxRep(map, prefix: "\(prefix).createPassiveSellOfferOp")
      return .createPassiveSellOfferOp(val)
    case "SET_OPTIONS":
      let val = try SetOptionsOperationXDR.fromTxRep(map, prefix: "\(prefix).setOptionsOp")
      return .setOptionsOp(val)
    case "CHANGE_TRUST":
      let val = try ChangeTrustOperationXDR.fromTxRep(map, prefix: "\(prefix).changeTrustOp")
      return .changeTrustOp(val)
    case "ALLOW_TRUST":
      let val = try AllowTrustOperationXDR.fromTxRep(map, prefix: "\(prefix).allowTrustOp")
      return .allowTrustOp(val)
    case "ACCOUNT_MERGE":
      let val = try TxRepHelper.requireMuxedAccount(map, "\(prefix).destination")
      return .accountMerge(val)
    case "INFLATION":
      return .inflation
    case "MANAGE_DATA":
      let val = try ManageDataOperationXDR.fromTxRep(map, prefix: "\(prefix).manageDataOp")
      return .manageDataOp(val)
    case "BUMP_SEQUENCE":
      let val = try BumpSequenceOperationXDR.fromTxRep(map, prefix: "\(prefix).bumpSequenceOp")
      return .bumpSequenceOp(val)
    case "MANAGE_BUY_OFFER":
      let selling: AssetXDR = try TxRepHelper.requireAsset(map, "\(prefix).manageBuyOfferOp.selling")
      let buying: AssetXDR = try TxRepHelper.requireAsset(map, "\(prefix).manageBuyOfferOp.buying")
      let amount: Int64 = try TxRepHelper.parseInt64(TxRepHelper.getValue(map, "\(prefix).manageBuyOfferOp.buyAmount") ?? "0")
      let price: PriceXDR = try PriceXDR.fromTxRep(map, prefix: "\(prefix).manageBuyOfferOp.price")
      let offerID: Int64 = try TxRepHelper.parseInt64(TxRepHelper.getValue(map, "\(prefix).manageBuyOfferOp.offerID") ?? "0")
      let val = ManageOfferOperationXDR(selling: selling, buying: buying, amount: amount, price: price, offerID: offerID)
      return .manageBuyOfferOp(val)
    case "PATH_PAYMENT_STRICT_SEND":
      let sendAsset: AssetXDR = try TxRepHelper.requireAsset(map, "\(prefix).pathPaymentStrictSendOp.sendAsset")
      let sendMax: Int64 = try TxRepHelper.parseInt64(TxRepHelper.getValue(map, "\(prefix).pathPaymentStrictSendOp.sendAmount") ?? "0")
      let destination: MuxedAccountXDR = try TxRepHelper.requireMuxedAccount(map, "\(prefix).pathPaymentStrictSendOp.destination")
      let destinationAsset: AssetXDR = try TxRepHelper.requireAsset(map, "\(prefix).pathPaymentStrictSendOp.destAsset")
      let destinationAmount: Int64 = try TxRepHelper.parseInt64(TxRepHelper.getValue(map, "\(prefix).pathPaymentStrictSendOp.destMin") ?? "0")
      let pathLen = try TxRepHelper.parseInt(TxRepHelper.getValue(map, "\(prefix).pathPaymentStrictSendOp.path.len") ?? "0")
      var path = [AssetXDR]()
      for i in 0..<Int(pathLen) {
        let item: AssetXDR = try TxRepHelper.parseAsset(TxRepHelper.getValue(map, "\(prefix).pathPaymentStrictSendOp.path[\(i)]") ?? "")
        path.append(item)
      }
      let val = PathPaymentOperationXDR(sendAsset: sendAsset, sendMax: sendMax, destination: destination, destinationAsset: destinationAsset, destinationAmount: destinationAmount, path: path)
      return .pathPaymentStrictSendOp(val)
    case "CREATE_CLAIMABLE_BALANCE":
      let val = try CreateClaimableBalanceOpXDR.fromTxRep(map, prefix: "\(prefix).createClaimableBalanceOp")
      return .createClaimableBalanceOp(val)
    case "CLAIM_CLAIMABLE_BALANCE":
      let val = try ClaimClaimableBalanceOpXDR.fromTxRep(map, prefix: "\(prefix).claimClaimableBalanceOp")
      return .claimClaimableBalanceOp(val)
    case "BEGIN_SPONSORING_FUTURE_RESERVES":
      let sponsoredId: PublicKey = try TxRepHelper.requireAccountId(map, "\(prefix).beginSponsoringFutureReservesOp.sponsoredID")
      let val = BeginSponsoringFutureReservesOpXDR(sponsoredId: sponsoredId)
      return .beginSponsoringFutureReservesOp(val)
    case "END_SPONSORING_FUTURE_RESERVES":
      return .endSponsoringFutureReserves
    case "REVOKE_SPONSORSHIP":
      let val = try RevokeSponsorshipOpXDR.fromTxRep(map, prefix: "\(prefix).revokeSponsorshipOp")
      return .revokeSponsorshipOp(val)
    case "CLAWBACK":
      let val = try ClawbackOpXDR.fromTxRep(map, prefix: "\(prefix).clawbackOp")
      return .clawbackOp(val)
    case "CLAWBACK_CLAIMABLE_BALANCE":
      let val = try ClawbackClaimableBalanceOpXDR.fromTxRep(map, prefix: "\(prefix).clawbackClaimableBalanceOp")
      return .clawbackClaimableBalanceOp(val)
    case "SET_TRUST_LINE_FLAGS":
      let accountID: PublicKey = try TxRepHelper.requireAccountId(map, "\(prefix).setTrustLineFlagsOp.trustor")
      let asset: AssetXDR = try TxRepHelper.requireAsset(map, "\(prefix).setTrustLineFlagsOp.asset")
      let clearFlags: UInt32 = UInt32(try TxRepHelper.parseUInt64(TxRepHelper.getValue(map, "\(prefix).setTrustLineFlagsOp.clearFlags") ?? "0"))
      let setFlags: UInt32 = UInt32(try TxRepHelper.parseUInt64(TxRepHelper.getValue(map, "\(prefix).setTrustLineFlagsOp.setFlags") ?? "0"))
      let val = SetTrustLineFlagsOpXDR(accountID: accountID, asset: asset, setFlags: setFlags, clearFlags: clearFlags)
      return .setTrustLineFlagsOp(val)
    case "LIQUIDITY_POOL_DEPOSIT":
      let val = try LiquidityPoolDepositOpXDR.fromTxRep(map, prefix: "\(prefix).liquidityPoolDepositOp")
      return .liquidityPoolDepositOp(val)
    case "LIQUIDITY_POOL_WITHDRAW":
      let val = try LiquidityPoolWithdrawOpXDR.fromTxRep(map, prefix: "\(prefix).liquidityPoolWithdrawOp")
      return .liquidityPoolWithdrawOp(val)
    case "INVOKE_HOST_FUNCTION":
      let val = try InvokeHostFunctionOpXDR.fromTxRep(map, prefix: "\(prefix).invokeHostFunctionOp")
      return .invokeHostFunctionOp(val)
    case "EXTEND_FOOTPRINT_TTL":
      let val = try ExtendFootprintTTLOpXDR.fromTxRep(map, prefix: "\(prefix).extendFootprintTTLOp")
      return .extendFootprintTTLOp(val)
    case "RESTORE_FOOTPRINT":
      let val = try RestoreFootprintOpXDR.fromTxRep(map, prefix: "\(prefix).restoreFootprintOp")
      return .restoreFootprintOp(val)
    default:
      throw TxRepError.invalidValue(key: discKey)
    }
  }
}

extension OperationBodyXDR: XdrJsonCodable {
  public func toXdrJsonValue() throws -> XdrJsonValue {
    switch self {
    case .createAccountOp(let payload):
      return .object([XdrJsonMember(key: "create_account", value: try payload.toXdrJsonValue())])
    case .paymentOp(let payload):
      return .object([XdrJsonMember(key: "payment", value: try payload.toXdrJsonValue())])
    case .pathPaymentStrictReceiveOp(let payload):
      return .object([XdrJsonMember(key: "path_payment_strict_receive", value: try Self.pathPaymentStrictReceiveOpToXdrJsonValue(payload))])
    case .manageSellOfferOp(let payload):
      return .object([XdrJsonMember(key: "manage_sell_offer", value: try Self.manageSellOfferOpToXdrJsonValue(payload))])
    case .createPassiveSellOfferOp(let payload):
      return .object([XdrJsonMember(key: "create_passive_sell_offer", value: try payload.toXdrJsonValue())])
    case .setOptionsOp(let payload):
      return .object([XdrJsonMember(key: "set_options", value: try payload.toXdrJsonValue())])
    case .changeTrustOp(let payload):
      return .object([XdrJsonMember(key: "change_trust", value: try payload.toXdrJsonValue())])
    case .allowTrustOp(let payload):
      return .object([XdrJsonMember(key: "allow_trust", value: try payload.toXdrJsonValue())])
    case .accountMerge(let payload):
      return .object([XdrJsonMember(key: "account_merge", value: try payload.toXdrJsonValue())])
    case .inflation: return .string("inflation")
    case .manageDataOp(let payload):
      return .object([XdrJsonMember(key: "manage_data", value: try payload.toXdrJsonValue())])
    case .bumpSequenceOp(let payload):
      return .object([XdrJsonMember(key: "bump_sequence", value: try payload.toXdrJsonValue())])
    case .manageBuyOfferOp(let payload):
      return .object([XdrJsonMember(key: "manage_buy_offer", value: try Self.manageBuyOfferOpToXdrJsonValue(payload))])
    case .pathPaymentStrictSendOp(let payload):
      return .object([XdrJsonMember(key: "path_payment_strict_send", value: try Self.pathPaymentStrictSendOpToXdrJsonValue(payload))])
    case .createClaimableBalanceOp(let payload):
      return .object([XdrJsonMember(key: "create_claimable_balance", value: try payload.toXdrJsonValue())])
    case .claimClaimableBalanceOp(let payload):
      return .object([XdrJsonMember(key: "claim_claimable_balance", value: try payload.toXdrJsonValue())])
    case .beginSponsoringFutureReservesOp(let payload):
      return .object([XdrJsonMember(key: "begin_sponsoring_future_reserves", value: try payload.toXdrJsonValue())])
    case .endSponsoringFutureReserves: return .string("end_sponsoring_future_reserves")
    case .revokeSponsorshipOp(let payload):
      return .object([XdrJsonMember(key: "revoke_sponsorship", value: try payload.toXdrJsonValue())])
    case .clawbackOp(let payload):
      return .object([XdrJsonMember(key: "clawback", value: try payload.toXdrJsonValue())])
    case .clawbackClaimableBalanceOp(let payload):
      return .object([XdrJsonMember(key: "clawback_claimable_balance", value: try payload.toXdrJsonValue())])
    case .setTrustLineFlagsOp(let payload):
      return .object([XdrJsonMember(key: "set_trust_line_flags", value: try payload.toXdrJsonValue())])
    case .liquidityPoolDepositOp(let payload):
      return .object([XdrJsonMember(key: "liquidity_pool_deposit", value: try payload.toXdrJsonValue())])
    case .liquidityPoolWithdrawOp(let payload):
      return .object([XdrJsonMember(key: "liquidity_pool_withdraw", value: try payload.toXdrJsonValue())])
    case .invokeHostFunctionOp(let payload):
      return .object([XdrJsonMember(key: "invoke_host_function", value: try payload.toXdrJsonValue())])
    case .extendFootprintTTLOp(let payload):
      return .object([XdrJsonMember(key: "extend_footprint_ttl", value: try payload.toXdrJsonValue())])
    case .restoreFootprintOp(let payload):
      return .object([XdrJsonMember(key: "restore_footprint", value: try payload.toXdrJsonValue())])
    }
  }

  public static func fromXdrJsonValue(_ value: XdrJsonValue) throws -> OperationBodyXDR {
    if case .string(let name) = value {
      switch name {
      case "create_account":
        throw XdrJsonError.invalidValue(type: "OperationBodyXDR", key: "create_account",
                                        message: "this arm carries a value, so it is written as a single-key object")
      case "payment":
        throw XdrJsonError.invalidValue(type: "OperationBodyXDR", key: "payment",
                                        message: "this arm carries a value, so it is written as a single-key object")
      case "path_payment_strict_receive":
        throw XdrJsonError.invalidValue(type: "OperationBodyXDR", key: "path_payment_strict_receive",
                                        message: "this arm carries a value, so it is written as a single-key object")
      case "manage_sell_offer":
        throw XdrJsonError.invalidValue(type: "OperationBodyXDR", key: "manage_sell_offer",
                                        message: "this arm carries a value, so it is written as a single-key object")
      case "create_passive_sell_offer":
        throw XdrJsonError.invalidValue(type: "OperationBodyXDR", key: "create_passive_sell_offer",
                                        message: "this arm carries a value, so it is written as a single-key object")
      case "set_options":
        throw XdrJsonError.invalidValue(type: "OperationBodyXDR", key: "set_options",
                                        message: "this arm carries a value, so it is written as a single-key object")
      case "change_trust":
        throw XdrJsonError.invalidValue(type: "OperationBodyXDR", key: "change_trust",
                                        message: "this arm carries a value, so it is written as a single-key object")
      case "allow_trust":
        throw XdrJsonError.invalidValue(type: "OperationBodyXDR", key: "allow_trust",
                                        message: "this arm carries a value, so it is written as a single-key object")
      case "account_merge":
        throw XdrJsonError.invalidValue(type: "OperationBodyXDR", key: "account_merge",
                                        message: "this arm carries a value, so it is written as a single-key object")
      case "inflation":
        return .inflation
      case "manage_data":
        throw XdrJsonError.invalidValue(type: "OperationBodyXDR", key: "manage_data",
                                        message: "this arm carries a value, so it is written as a single-key object")
      case "bump_sequence":
        throw XdrJsonError.invalidValue(type: "OperationBodyXDR", key: "bump_sequence",
                                        message: "this arm carries a value, so it is written as a single-key object")
      case "manage_buy_offer":
        throw XdrJsonError.invalidValue(type: "OperationBodyXDR", key: "manage_buy_offer",
                                        message: "this arm carries a value, so it is written as a single-key object")
      case "path_payment_strict_send":
        throw XdrJsonError.invalidValue(type: "OperationBodyXDR", key: "path_payment_strict_send",
                                        message: "this arm carries a value, so it is written as a single-key object")
      case "create_claimable_balance":
        throw XdrJsonError.invalidValue(type: "OperationBodyXDR", key: "create_claimable_balance",
                                        message: "this arm carries a value, so it is written as a single-key object")
      case "claim_claimable_balance":
        throw XdrJsonError.invalidValue(type: "OperationBodyXDR", key: "claim_claimable_balance",
                                        message: "this arm carries a value, so it is written as a single-key object")
      case "begin_sponsoring_future_reserves":
        throw XdrJsonError.invalidValue(type: "OperationBodyXDR", key: "begin_sponsoring_future_reserves",
                                        message: "this arm carries a value, so it is written as a single-key object")
      case "end_sponsoring_future_reserves":
        return .endSponsoringFutureReserves
      case "revoke_sponsorship":
        throw XdrJsonError.invalidValue(type: "OperationBodyXDR", key: "revoke_sponsorship",
                                        message: "this arm carries a value, so it is written as a single-key object")
      case "clawback":
        throw XdrJsonError.invalidValue(type: "OperationBodyXDR", key: "clawback",
                                        message: "this arm carries a value, so it is written as a single-key object")
      case "clawback_claimable_balance":
        throw XdrJsonError.invalidValue(type: "OperationBodyXDR", key: "clawback_claimable_balance",
                                        message: "this arm carries a value, so it is written as a single-key object")
      case "set_trust_line_flags":
        throw XdrJsonError.invalidValue(type: "OperationBodyXDR", key: "set_trust_line_flags",
                                        message: "this arm carries a value, so it is written as a single-key object")
      case "liquidity_pool_deposit":
        throw XdrJsonError.invalidValue(type: "OperationBodyXDR", key: "liquidity_pool_deposit",
                                        message: "this arm carries a value, so it is written as a single-key object")
      case "liquidity_pool_withdraw":
        throw XdrJsonError.invalidValue(type: "OperationBodyXDR", key: "liquidity_pool_withdraw",
                                        message: "this arm carries a value, so it is written as a single-key object")
      case "invoke_host_function":
        throw XdrJsonError.invalidValue(type: "OperationBodyXDR", key: "invoke_host_function",
                                        message: "this arm carries a value, so it is written as a single-key object")
      case "extend_footprint_ttl":
        throw XdrJsonError.invalidValue(type: "OperationBodyXDR", key: "extend_footprint_ttl",
                                        message: "this arm carries a value, so it is written as a single-key object")
      case "restore_footprint":
        throw XdrJsonError.invalidValue(type: "OperationBodyXDR", key: "restore_footprint",
                                        message: "this arm carries a value, so it is written as a single-key object")
      default:
        throw XdrJsonError.unknownUnionArm(type: "OperationBodyXDR", key: name)
      }
    }

    let member = try XdrJson.singleKeyObject(value, type: "OperationBodyXDR")
    switch member.key {
    case "create_account":
      let createAccountOp: CreateAccountOperationXDR = try CreateAccountOperationXDR.fromXdrJsonValue(member.value)
      return .createAccountOp(createAccountOp)
    case "payment":
      let paymentOp: PaymentOperationXDR = try PaymentOperationXDR.fromXdrJsonValue(member.value)
      return .paymentOp(paymentOp)
    case "path_payment_strict_receive":
      let pathPaymentStrictReceiveOp: PathPaymentOperationXDR = try Self.pathPaymentStrictReceiveOpFromXdrJsonValue(member.value)
      return .pathPaymentStrictReceiveOp(pathPaymentStrictReceiveOp)
    case "manage_sell_offer":
      let manageSellOfferOp: ManageOfferOperationXDR = try Self.manageSellOfferOpFromXdrJsonValue(member.value)
      return .manageSellOfferOp(manageSellOfferOp)
    case "create_passive_sell_offer":
      let createPassiveSellOfferOp: CreatePassiveOfferOperationXDR = try CreatePassiveOfferOperationXDR.fromXdrJsonValue(member.value)
      return .createPassiveSellOfferOp(createPassiveSellOfferOp)
    case "set_options":
      let setOptionsOp: SetOptionsOperationXDR = try SetOptionsOperationXDR.fromXdrJsonValue(member.value)
      return .setOptionsOp(setOptionsOp)
    case "change_trust":
      let changeTrustOp: ChangeTrustOperationXDR = try ChangeTrustOperationXDR.fromXdrJsonValue(member.value)
      return .changeTrustOp(changeTrustOp)
    case "allow_trust":
      let allowTrustOp: AllowTrustOperationXDR = try AllowTrustOperationXDR.fromXdrJsonValue(member.value)
      return .allowTrustOp(allowTrustOp)
    case "account_merge":
      let accountMerge: MuxedAccountXDR = try MuxedAccountXDR.fromXdrJsonValue(member.value)
      return .accountMerge(accountMerge)
    case "inflation":
      throw XdrJsonError.invalidValue(type: "OperationBodyXDR", key: "inflation",
                                      message: "this arm carries no value, so it is written as a bare string")
    case "manage_data":
      let manageDataOp: ManageDataOperationXDR = try ManageDataOperationXDR.fromXdrJsonValue(member.value)
      return .manageDataOp(manageDataOp)
    case "bump_sequence":
      let bumpSequenceOp: BumpSequenceOperationXDR = try BumpSequenceOperationXDR.fromXdrJsonValue(member.value)
      return .bumpSequenceOp(bumpSequenceOp)
    case "manage_buy_offer":
      let manageBuyOfferOp: ManageOfferOperationXDR = try Self.manageBuyOfferOpFromXdrJsonValue(member.value)
      return .manageBuyOfferOp(manageBuyOfferOp)
    case "path_payment_strict_send":
      let pathPaymentStrictSendOp: PathPaymentOperationXDR = try Self.pathPaymentStrictSendOpFromXdrJsonValue(member.value)
      return .pathPaymentStrictSendOp(pathPaymentStrictSendOp)
    case "create_claimable_balance":
      let createClaimableBalanceOp: CreateClaimableBalanceOpXDR = try CreateClaimableBalanceOpXDR.fromXdrJsonValue(member.value)
      return .createClaimableBalanceOp(createClaimableBalanceOp)
    case "claim_claimable_balance":
      let claimClaimableBalanceOp: ClaimClaimableBalanceOpXDR = try ClaimClaimableBalanceOpXDR.fromXdrJsonValue(member.value)
      return .claimClaimableBalanceOp(claimClaimableBalanceOp)
    case "begin_sponsoring_future_reserves":
      let beginSponsoringFutureReservesOp: BeginSponsoringFutureReservesOpXDR = try BeginSponsoringFutureReservesOpXDR.fromXdrJsonValue(member.value)
      return .beginSponsoringFutureReservesOp(beginSponsoringFutureReservesOp)
    case "end_sponsoring_future_reserves":
      throw XdrJsonError.invalidValue(type: "OperationBodyXDR", key: "end_sponsoring_future_reserves",
                                      message: "this arm carries no value, so it is written as a bare string")
    case "revoke_sponsorship":
      let revokeSponsorshipOp: RevokeSponsorshipOpXDR = try RevokeSponsorshipOpXDR.fromXdrJsonValue(member.value)
      return .revokeSponsorshipOp(revokeSponsorshipOp)
    case "clawback":
      let clawbackOp: ClawbackOpXDR = try ClawbackOpXDR.fromXdrJsonValue(member.value)
      return .clawbackOp(clawbackOp)
    case "clawback_claimable_balance":
      let clawbackClaimableBalanceOp: ClawbackClaimableBalanceOpXDR = try ClawbackClaimableBalanceOpXDR.fromXdrJsonValue(member.value)
      return .clawbackClaimableBalanceOp(clawbackClaimableBalanceOp)
    case "set_trust_line_flags":
      let setTrustLineFlagsOp: SetTrustLineFlagsOpXDR = try SetTrustLineFlagsOpXDR.fromXdrJsonValue(member.value)
      return .setTrustLineFlagsOp(setTrustLineFlagsOp)
    case "liquidity_pool_deposit":
      let liquidityPoolDepositOp: LiquidityPoolDepositOpXDR = try LiquidityPoolDepositOpXDR.fromXdrJsonValue(member.value)
      return .liquidityPoolDepositOp(liquidityPoolDepositOp)
    case "liquidity_pool_withdraw":
      let liquidityPoolWithdrawOp: LiquidityPoolWithdrawOpXDR = try LiquidityPoolWithdrawOpXDR.fromXdrJsonValue(member.value)
      return .liquidityPoolWithdrawOp(liquidityPoolWithdrawOp)
    case "invoke_host_function":
      let invokeHostFunctionOp: InvokeHostFunctionOpXDR = try InvokeHostFunctionOpXDR.fromXdrJsonValue(member.value)
      return .invokeHostFunctionOp(invokeHostFunctionOp)
    case "extend_footprint_ttl":
      let extendFootprintTTLOp: ExtendFootprintTTLOpXDR = try ExtendFootprintTTLOpXDR.fromXdrJsonValue(member.value)
      return .extendFootprintTTLOp(extendFootprintTTLOp)
    case "restore_footprint":
      let restoreFootprintOp: RestoreFootprintOpXDR = try RestoreFootprintOpXDR.fromXdrJsonValue(member.value)
      return .restoreFootprintOp(restoreFootprintOp)
    default:
      throw XdrJsonError.unknownUnionArm(type: "OperationBodyXDR", key: member.key)
    }
  }

  // MARK: - PathPaymentStrictReceiveOp

  private static func pathPaymentStrictReceiveOpToXdrJsonValue(_ value: PathPaymentOperationXDR) throws -> XdrJsonValue {
    var members: [XdrJsonMember] = []
    members.append(XdrJsonMember(key: "send_asset", value: try value.sendAsset.toXdrJsonValue()))
    members.append(XdrJsonMember(key: "send_max", value: try Int64XDRJsonCodec.toXdrJsonValue(value.sendMax, type: "PathPaymentStrictReceiveOp", key: "send_max")))
    members.append(XdrJsonMember(key: "destination", value: try value.destination.toXdrJsonValue()))
    members.append(XdrJsonMember(key: "dest_asset", value: try value.destinationAsset.toXdrJsonValue()))
    members.append(XdrJsonMember(key: "dest_amount", value: try Int64XDRJsonCodec.toXdrJsonValue(value.destinationAmount, type: "PathPaymentStrictReceiveOp", key: "dest_amount")))
    members.append(XdrJsonMember(key: "path", value: try XdrJson.array(value.path.map { element in try element.toXdrJsonValue() })))
    return .object(members)
  }

  private static func pathPaymentStrictReceiveOpFromXdrJsonValue(_ value: XdrJsonValue) throws -> PathPaymentOperationXDR {
    let members = try XdrJson.object(value, type: "PathPaymentStrictReceiveOp", keys: ["send_asset", "send_max", "destination", "dest_asset", "dest_amount", "path"])
    let sendAsset: AssetXDR = try AssetXDR.fromXdrJsonValue(try XdrJson.field(members, key: "send_asset", type: "PathPaymentStrictReceiveOp"))
    let sendMax: Int64 = try Int64XDRJsonCodec.fromXdrJsonValue(try XdrJson.field(members, key: "send_max", type: "PathPaymentStrictReceiveOp"), type: "PathPaymentStrictReceiveOp", key: "send_max")
    let destination: MuxedAccountXDR = try MuxedAccountXDR.fromXdrJsonValue(try XdrJson.field(members, key: "destination", type: "PathPaymentStrictReceiveOp"))
    let destinationAsset: AssetXDR = try AssetXDR.fromXdrJsonValue(try XdrJson.field(members, key: "dest_asset", type: "PathPaymentStrictReceiveOp"))
    let destinationAmount: Int64 = try Int64XDRJsonCodec.fromXdrJsonValue(try XdrJson.field(members, key: "dest_amount", type: "PathPaymentStrictReceiveOp"), type: "PathPaymentStrictReceiveOp", key: "dest_amount")
    let pathElements = try XdrJson.array(try XdrJson.field(members, key: "path", type: "PathPaymentStrictReceiveOp"), type: "PathPaymentStrictReceiveOp", key: "path")
    let path: [AssetXDR] = try pathElements.map { element in try AssetXDR.fromXdrJsonValue(element) }
    return PathPaymentOperationXDR(
      sendAsset: sendAsset,
      sendMax: sendMax,
      destination: destination,
      destinationAsset: destinationAsset,
      destinationAmount: destinationAmount,
      path: path
    )
  }

  // MARK: - ManageSellOfferOp

  private static func manageSellOfferOpToXdrJsonValue(_ value: ManageOfferOperationXDR) throws -> XdrJsonValue {
    var members: [XdrJsonMember] = []
    members.append(XdrJsonMember(key: "selling", value: try value.selling.toXdrJsonValue()))
    members.append(XdrJsonMember(key: "buying", value: try value.buying.toXdrJsonValue()))
    members.append(XdrJsonMember(key: "amount", value: try Int64XDRJsonCodec.toXdrJsonValue(value.amount, type: "ManageSellOfferOp", key: "amount")))
    members.append(XdrJsonMember(key: "price", value: try value.price.toXdrJsonValue()))
    members.append(XdrJsonMember(key: "offer_id", value: try Int64XDRJsonCodec.toXdrJsonValue(value.offerID, type: "ManageSellOfferOp", key: "offer_id")))
    return .object(members)
  }

  private static func manageSellOfferOpFromXdrJsonValue(_ value: XdrJsonValue) throws -> ManageOfferOperationXDR {
    let members = try XdrJson.object(value, type: "ManageSellOfferOp", keys: ["selling", "buying", "amount", "price", "offer_id"])
    let selling: AssetXDR = try AssetXDR.fromXdrJsonValue(try XdrJson.field(members, key: "selling", type: "ManageSellOfferOp"))
    let buying: AssetXDR = try AssetXDR.fromXdrJsonValue(try XdrJson.field(members, key: "buying", type: "ManageSellOfferOp"))
    let amount: Int64 = try Int64XDRJsonCodec.fromXdrJsonValue(try XdrJson.field(members, key: "amount", type: "ManageSellOfferOp"), type: "ManageSellOfferOp", key: "amount")
    let price: PriceXDR = try PriceXDR.fromXdrJsonValue(try XdrJson.field(members, key: "price", type: "ManageSellOfferOp"))
    let offerID: Int64 = try Int64XDRJsonCodec.fromXdrJsonValue(try XdrJson.field(members, key: "offer_id", type: "ManageSellOfferOp"), type: "ManageSellOfferOp", key: "offer_id")
    return ManageOfferOperationXDR(
      selling: selling,
      buying: buying,
      amount: amount,
      price: price,
      offerID: offerID
    )
  }

  // MARK: - ManageBuyOfferOp

  private static func manageBuyOfferOpToXdrJsonValue(_ value: ManageOfferOperationXDR) throws -> XdrJsonValue {
    var members: [XdrJsonMember] = []
    members.append(XdrJsonMember(key: "selling", value: try value.selling.toXdrJsonValue()))
    members.append(XdrJsonMember(key: "buying", value: try value.buying.toXdrJsonValue()))
    members.append(XdrJsonMember(key: "buy_amount", value: try Int64XDRJsonCodec.toXdrJsonValue(value.amount, type: "ManageBuyOfferOp", key: "buy_amount")))
    members.append(XdrJsonMember(key: "price", value: try value.price.toXdrJsonValue()))
    members.append(XdrJsonMember(key: "offer_id", value: try Int64XDRJsonCodec.toXdrJsonValue(value.offerID, type: "ManageBuyOfferOp", key: "offer_id")))
    return .object(members)
  }

  private static func manageBuyOfferOpFromXdrJsonValue(_ value: XdrJsonValue) throws -> ManageOfferOperationXDR {
    let members = try XdrJson.object(value, type: "ManageBuyOfferOp", keys: ["selling", "buying", "buy_amount", "price", "offer_id"])
    let selling: AssetXDR = try AssetXDR.fromXdrJsonValue(try XdrJson.field(members, key: "selling", type: "ManageBuyOfferOp"))
    let buying: AssetXDR = try AssetXDR.fromXdrJsonValue(try XdrJson.field(members, key: "buying", type: "ManageBuyOfferOp"))
    let amount: Int64 = try Int64XDRJsonCodec.fromXdrJsonValue(try XdrJson.field(members, key: "buy_amount", type: "ManageBuyOfferOp"), type: "ManageBuyOfferOp", key: "buy_amount")
    let price: PriceXDR = try PriceXDR.fromXdrJsonValue(try XdrJson.field(members, key: "price", type: "ManageBuyOfferOp"))
    let offerID: Int64 = try Int64XDRJsonCodec.fromXdrJsonValue(try XdrJson.field(members, key: "offer_id", type: "ManageBuyOfferOp"), type: "ManageBuyOfferOp", key: "offer_id")
    return ManageOfferOperationXDR(
      selling: selling,
      buying: buying,
      amount: amount,
      price: price,
      offerID: offerID
    )
  }

  // MARK: - PathPaymentStrictSendOp

  private static func pathPaymentStrictSendOpToXdrJsonValue(_ value: PathPaymentOperationXDR) throws -> XdrJsonValue {
    var members: [XdrJsonMember] = []
    members.append(XdrJsonMember(key: "send_asset", value: try value.sendAsset.toXdrJsonValue()))
    members.append(XdrJsonMember(key: "send_amount", value: try Int64XDRJsonCodec.toXdrJsonValue(value.sendMax, type: "PathPaymentStrictSendOp", key: "send_amount")))
    members.append(XdrJsonMember(key: "destination", value: try value.destination.toXdrJsonValue()))
    members.append(XdrJsonMember(key: "dest_asset", value: try value.destinationAsset.toXdrJsonValue()))
    members.append(XdrJsonMember(key: "dest_min", value: try Int64XDRJsonCodec.toXdrJsonValue(value.destinationAmount, type: "PathPaymentStrictSendOp", key: "dest_min")))
    members.append(XdrJsonMember(key: "path", value: try XdrJson.array(value.path.map { element in try element.toXdrJsonValue() })))
    return .object(members)
  }

  private static func pathPaymentStrictSendOpFromXdrJsonValue(_ value: XdrJsonValue) throws -> PathPaymentOperationXDR {
    let members = try XdrJson.object(value, type: "PathPaymentStrictSendOp", keys: ["send_asset", "send_amount", "destination", "dest_asset", "dest_min", "path"])
    let sendAsset: AssetXDR = try AssetXDR.fromXdrJsonValue(try XdrJson.field(members, key: "send_asset", type: "PathPaymentStrictSendOp"))
    let sendMax: Int64 = try Int64XDRJsonCodec.fromXdrJsonValue(try XdrJson.field(members, key: "send_amount", type: "PathPaymentStrictSendOp"), type: "PathPaymentStrictSendOp", key: "send_amount")
    let destination: MuxedAccountXDR = try MuxedAccountXDR.fromXdrJsonValue(try XdrJson.field(members, key: "destination", type: "PathPaymentStrictSendOp"))
    let destinationAsset: AssetXDR = try AssetXDR.fromXdrJsonValue(try XdrJson.field(members, key: "dest_asset", type: "PathPaymentStrictSendOp"))
    let destinationAmount: Int64 = try Int64XDRJsonCodec.fromXdrJsonValue(try XdrJson.field(members, key: "dest_min", type: "PathPaymentStrictSendOp"), type: "PathPaymentStrictSendOp", key: "dest_min")
    let pathElements = try XdrJson.array(try XdrJson.field(members, key: "path", type: "PathPaymentStrictSendOp"), type: "PathPaymentStrictSendOp", key: "path")
    let path: [AssetXDR] = try pathElements.map { element in try AssetXDR.fromXdrJsonValue(element) }
    return PathPaymentOperationXDR(
      sendAsset: sendAsset,
      sendMax: sendMax,
      destination: destination,
      destinationAsset: destinationAsset,
      destinationAmount: destinationAmount,
      path: path
    )
  }
}
