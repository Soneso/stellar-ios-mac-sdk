// This file was automatically generated by xdrgen.
// DO NOT EDIT or your changes may be overwritten.

import Foundation

public struct Int256PartsXDR: XDRCodable, Sendable {
  public var hiHi: Int64
  public var hiLo: UInt64
  public var loHi: UInt64
  public var loLo: UInt64

  public init(
    hiHi: Int64,
    hiLo: UInt64,
    loHi: UInt64,
    loLo: UInt64
  ) {
    self.hiHi = hiHi
    self.hiLo = hiLo
    self.loHi = loHi
    self.loLo = loLo
  }

  public init(from decoder: Decoder) throws {
    var container = try decoder.unkeyedContainer()
    hiHi = try container.decode(Int64.self)
    hiLo = try container.decode(UInt64.self)
    loHi = try container.decode(UInt64.self)
    loLo = try container.decode(UInt64.self)
  }

  public func encode(to encoder: Encoder) throws {
    var container = encoder.unkeyedContainer()
    try container.encode(hiHi)
    try container.encode(hiLo)
    try container.encode(loHi)
    try container.encode(loLo)
  }
}

extension Int256PartsXDR {
  public func toTxRep(prefix: String, lines: inout [String]) throws {
    lines.append("\(prefix).hi_hi: \(self.hiHi)")
    lines.append("\(prefix).hi_lo: \(self.hiLo)")
    lines.append("\(prefix).lo_hi: \(self.loHi)")
    lines.append("\(prefix).lo_lo: \(self.loLo)")
  }

  public static func fromTxRep(_ map: [String: String], prefix: String) throws -> Int256PartsXDR {
    let hiHi: Int64 = try TxRepHelper.parseInt64(TxRepHelper.getValue(map, "\(prefix).hi_hi") ?? "0")
    let hiLo: UInt64 = try TxRepHelper.parseUInt64(TxRepHelper.getValue(map, "\(prefix).hi_lo") ?? "0")
    let loHi: UInt64 = try TxRepHelper.parseUInt64(TxRepHelper.getValue(map, "\(prefix).lo_hi") ?? "0")
    let loLo: UInt64 = try TxRepHelper.parseUInt64(TxRepHelper.getValue(map, "\(prefix).lo_lo") ?? "0")
    return Int256PartsXDR(hiHi: hiHi, hiLo: hiLo, loHi: loHi, loLo: loLo)
  }
}

extension Int256PartsXDR: XdrJsonCodable {
  public func toXdrJsonValue() throws -> XdrJsonValue {
    return XdrJson.wideDecimal(
      XdrWideInteger.data256(hiHi: UInt64(bitPattern: self.hiHi), hiLo: self.hiLo,
                             loHi: self.loHi, loLo: self.loLo), signed: true)
  }

  public static func fromXdrJsonValue(_ value: XdrJsonValue) throws -> Int256PartsXDR {
    let bytes = try XdrJson.wideDecimal(value, bitSize: 256, signed: true, type: "Int256PartsXDR")
    let parts = XdrWideInteger.parts256(from: bytes)
    return Int256PartsXDR(hiHi: Int64(bitPattern: parts.hiHi), hiLo: parts.hiLo,
                          loHi: parts.loHi, loLo: parts.loLo)
  }
}
