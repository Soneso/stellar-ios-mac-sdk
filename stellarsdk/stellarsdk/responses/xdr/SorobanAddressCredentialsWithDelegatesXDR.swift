// This file was automatically generated by xdrgen.
// DO NOT EDIT or your changes may be overwritten.

import Foundation

public struct SorobanAddressCredentialsWithDelegatesXDR: XDRCodable, Sendable {
  public var addressCredentials: SorobanAddressCredentialsXDR
  public var delegates: [SorobanDelegateSignatureXDR]

  public init(addressCredentials: SorobanAddressCredentialsXDR, delegates: [SorobanDelegateSignatureXDR]) {
    self.addressCredentials = addressCredentials
    self.delegates = delegates
  }

  public init(from decoder: Decoder) throws {
    var container = try decoder.unkeyedContainer()
    addressCredentials = try container.decode(SorobanAddressCredentialsXDR.self)
    delegates = try decodeArray(type: SorobanDelegateSignatureXDR.self, dec: decoder)
  }

  public func encode(to encoder: Encoder) throws {
    var container = encoder.unkeyedContainer()
    try container.encode(addressCredentials)
    try container.encode(delegates)
  }
}

extension SorobanAddressCredentialsWithDelegatesXDR {
  public func toTxRep(prefix: String, lines: inout [String]) throws {
    try self.addressCredentials.toTxRep(prefix: "\(prefix).addressCredentials", lines: &lines)
    lines.append("\(prefix).delegates.len: \(self.delegates.count)")
    for (i, item) in self.delegates.enumerated() {
      try item.toTxRep(prefix: "\(prefix).delegates[\(i)]", lines: &lines)
    }
  }

  public static func fromTxRep(_ map: [String: String], prefix: String) throws -> SorobanAddressCredentialsWithDelegatesXDR {
    let addressCredentials: SorobanAddressCredentialsXDR = try SorobanAddressCredentialsXDR.fromTxRep(map, prefix: "\(prefix).addressCredentials")
    let delegatesLen = try TxRepHelper.parseInt(TxRepHelper.getValue(map, "\(prefix).delegates.len") ?? "0")
    var delegates = [SorobanDelegateSignatureXDR]()
    for i in 0..<Int(delegatesLen) {
      let item: SorobanDelegateSignatureXDR = try SorobanDelegateSignatureXDR.fromTxRep(map, prefix: "\(prefix).delegates[\(i)]")
      delegates.append(item)
    }
    return SorobanAddressCredentialsWithDelegatesXDR(addressCredentials: addressCredentials, delegates: delegates)
  }
}

extension SorobanAddressCredentialsWithDelegatesXDR: XdrJsonCodable {
  public func toXdrJsonValue() throws -> XdrJsonValue {
    var members: [XdrJsonMember] = []
    members.append(XdrJsonMember(key: "address_credentials", value: try self.addressCredentials.toXdrJsonValue()))
    members.append(XdrJsonMember(key: "delegates", value: try XdrJson.array(self.delegates.map { element in try element.toXdrJsonValue() })))
    return .object(members)
  }

  public static func fromXdrJsonValue(_ value: XdrJsonValue) throws -> SorobanAddressCredentialsWithDelegatesXDR {
    let members = try XdrJson.object(value, type: "SorobanAddressCredentialsWithDelegatesXDR", keys: ["address_credentials", "delegates"])
    let addressCredentials: SorobanAddressCredentialsXDR = try SorobanAddressCredentialsXDR.fromXdrJsonValue(try XdrJson.field(members, key: "address_credentials", type: "SorobanAddressCredentialsWithDelegatesXDR"))
    let delegatesElements = try XdrJson.array(try XdrJson.field(members, key: "delegates", type: "SorobanAddressCredentialsWithDelegatesXDR"), type: "SorobanAddressCredentialsWithDelegatesXDR", key: "delegates")
    let delegates: [SorobanDelegateSignatureXDR] = try delegatesElements.map { element in try SorobanDelegateSignatureXDR.fromXdrJsonValue(element) }
    return SorobanAddressCredentialsWithDelegatesXDR(addressCredentials: addressCredentials, delegates: delegates)
  }
}
