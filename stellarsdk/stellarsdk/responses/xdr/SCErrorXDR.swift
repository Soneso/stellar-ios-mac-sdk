// This file was automatically generated by xdrgen.
// DO NOT EDIT or your changes may be overwritten.

import Foundation

public enum SCErrorXDR: XDRCodable, Sendable {
  case contract(UInt32)
  case wasmVm(SCErrorCode)
  case context(SCErrorCode)
  case storage(SCErrorCode)
  case object(SCErrorCode)
  case crypto(SCErrorCode)
  case events(SCErrorCode)
  case budget(SCErrorCode)
  case value(SCErrorCode)
  case auth(SCErrorCode)

  public init(from decoder: Decoder) throws {
    var container = try decoder.unkeyedContainer()
    let discriminant = try container.decode(Int32.self)

    switch discriminant {
    case SCErrorType.contract.rawValue:
      let val = try container.decode(UInt32.self)
      self = .contract(val)
    case SCErrorType.wasmVm.rawValue:
      let val = try container.decode(SCErrorCode.self)
      self = .wasmVm(val)
    case SCErrorType.context.rawValue:
      let val = try container.decode(SCErrorCode.self)
      self = .context(val)
    case SCErrorType.storage.rawValue:
      let val = try container.decode(SCErrorCode.self)
      self = .storage(val)
    case SCErrorType.object.rawValue:
      let val = try container.decode(SCErrorCode.self)
      self = .object(val)
    case SCErrorType.crypto.rawValue:
      let val = try container.decode(SCErrorCode.self)
      self = .crypto(val)
    case SCErrorType.events.rawValue:
      let val = try container.decode(SCErrorCode.self)
      self = .events(val)
    case SCErrorType.budget.rawValue:
      let val = try container.decode(SCErrorCode.self)
      self = .budget(val)
    case SCErrorType.value.rawValue:
      let val = try container.decode(SCErrorCode.self)
      self = .value(val)
    case SCErrorType.auth.rawValue:
      let val = try container.decode(SCErrorCode.self)
      self = .auth(val)
    default:
      throw StellarSDKError.xdrDecodingError(message: "Unknown SCErrorXDR discriminant: \(discriminant)")
    }
  }

  public func type() -> Int32 {
    switch self {
    case .contract: return SCErrorType.contract.rawValue
    case .wasmVm: return SCErrorType.wasmVm.rawValue
    case .context: return SCErrorType.context.rawValue
    case .storage: return SCErrorType.storage.rawValue
    case .object: return SCErrorType.object.rawValue
    case .crypto: return SCErrorType.crypto.rawValue
    case .events: return SCErrorType.events.rawValue
    case .budget: return SCErrorType.budget.rawValue
    case .value: return SCErrorType.value.rawValue
    case .auth: return SCErrorType.auth.rawValue
    }
  }

  public func encode(to encoder: Encoder) throws {
    var container = encoder.unkeyedContainer()
    try container.encode(type())

    switch self {
    case .contract(let val):
      try container.encode(val)
    case .wasmVm(let val):
      try container.encode(val)
    case .context(let val):
      try container.encode(val)
    case .storage(let val):
      try container.encode(val)
    case .object(let val):
      try container.encode(val)
    case .crypto(let val):
      try container.encode(val)
    case .events(let val):
      try container.encode(val)
    case .budget(let val):
      try container.encode(val)
    case .value(let val):
      try container.encode(val)
    case .auth(let val):
      try container.encode(val)
    }
  }
}

extension SCErrorXDR {
  public func toTxRep(prefix: String, lines: inout [String]) throws {
    switch self {
    case .contract(let val):
      lines.append("\(prefix).type: SCE_CONTRACT")
      lines.append("\(prefix).contractCode: \(val)")
    case .wasmVm(let val):
      lines.append("\(prefix).type: SCE_WASM_VM")
      try val.toTxRep(prefix: "\(prefix).code", lines: &lines)
    case .context(let val):
      lines.append("\(prefix).type: SCE_CONTEXT")
      try val.toTxRep(prefix: "\(prefix).code", lines: &lines)
    case .storage(let val):
      lines.append("\(prefix).type: SCE_STORAGE")
      try val.toTxRep(prefix: "\(prefix).code", lines: &lines)
    case .object(let val):
      lines.append("\(prefix).type: SCE_OBJECT")
      try val.toTxRep(prefix: "\(prefix).code", lines: &lines)
    case .crypto(let val):
      lines.append("\(prefix).type: SCE_CRYPTO")
      try val.toTxRep(prefix: "\(prefix).code", lines: &lines)
    case .events(let val):
      lines.append("\(prefix).type: SCE_EVENTS")
      try val.toTxRep(prefix: "\(prefix).code", lines: &lines)
    case .budget(let val):
      lines.append("\(prefix).type: SCE_BUDGET")
      try val.toTxRep(prefix: "\(prefix).code", lines: &lines)
    case .value(let val):
      lines.append("\(prefix).type: SCE_VALUE")
      try val.toTxRep(prefix: "\(prefix).code", lines: &lines)
    case .auth(let val):
      lines.append("\(prefix).type: SCE_AUTH")
      try val.toTxRep(prefix: "\(prefix).code", lines: &lines)
    }
  }

  public static func fromTxRep(_ map: [String: String], prefix: String) throws -> SCErrorXDR {
    let discKey = "\(prefix).type"
    guard let discName = TxRepHelper.getValue(map, discKey) else {
      throw TxRepError.missingValue(key: discKey)
    }
    switch discName {
    case "SCE_CONTRACT":
      let val = UInt32(try TxRepHelper.parseUInt64(TxRepHelper.getValue(map, "\(prefix).contractCode") ?? "0"))
      return .contract(val)
    case "SCE_WASM_VM":
      let val = try SCErrorCode.fromTxRep(map, prefix: "\(prefix).code")
      return .wasmVm(val)
    case "SCE_CONTEXT":
      let val = try SCErrorCode.fromTxRep(map, prefix: "\(prefix).code")
      return .context(val)
    case "SCE_STORAGE":
      let val = try SCErrorCode.fromTxRep(map, prefix: "\(prefix).code")
      return .storage(val)
    case "SCE_OBJECT":
      let val = try SCErrorCode.fromTxRep(map, prefix: "\(prefix).code")
      return .object(val)
    case "SCE_CRYPTO":
      let val = try SCErrorCode.fromTxRep(map, prefix: "\(prefix).code")
      return .crypto(val)
    case "SCE_EVENTS":
      let val = try SCErrorCode.fromTxRep(map, prefix: "\(prefix).code")
      return .events(val)
    case "SCE_BUDGET":
      let val = try SCErrorCode.fromTxRep(map, prefix: "\(prefix).code")
      return .budget(val)
    case "SCE_VALUE":
      let val = try SCErrorCode.fromTxRep(map, prefix: "\(prefix).code")
      return .value(val)
    case "SCE_AUTH":
      let val = try SCErrorCode.fromTxRep(map, prefix: "\(prefix).code")
      return .auth(val)
    default:
      throw TxRepError.invalidValue(key: discKey)
    }
  }
}
