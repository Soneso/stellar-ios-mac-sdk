// This file was automatically generated by xdrgen.
// DO NOT EDIT or your changes may be overwritten.

import Foundation

public enum LiquidityPoolParametersXDR: XDRCodable, Sendable {
  case constantProduct(LiquidityPoolConstantProductParametersXDR)

  public init(from decoder: Decoder) throws {
    var container = try decoder.unkeyedContainer()
    let discriminant = try container.decode(Int32.self)

    switch discriminant {
    case LiquidityPoolType.constantProduct.rawValue:
      let val = try container.decode(LiquidityPoolConstantProductParametersXDR.self)
      self = .constantProduct(val)
    default:
      throw StellarSDKError.xdrDecodingError(message: "Unknown LiquidityPoolParametersXDR discriminant: \(discriminant)")
    }
  }

  public func type() -> Int32 {
    switch self {
    case .constantProduct: return LiquidityPoolType.constantProduct.rawValue
    }
  }

  public func encode(to encoder: Encoder) throws {
    var container = encoder.unkeyedContainer()
    try container.encode(type())

    switch self {
    case .constantProduct(let val):
      try container.encode(val)
    }
  }
}

extension LiquidityPoolParametersXDR {
  public func toTxRep(prefix: String, lines: inout [String]) throws {
    switch self {
    case .constantProduct(let val):
      lines.append("\(prefix).type: LIQUIDITY_POOL_CONSTANT_PRODUCT")
      try val.toTxRep(prefix: "\(prefix).constantProduct", lines: &lines)
    }
  }

  public static func fromTxRep(_ map: [String: String], prefix: String) throws -> LiquidityPoolParametersXDR {
    let discKey = "\(prefix).type"
    guard let discName = TxRepHelper.getValue(map, discKey) else {
      throw TxRepError.missingValue(key: discKey)
    }
    switch discName {
    case "LIQUIDITY_POOL_CONSTANT_PRODUCT":
      let val = try LiquidityPoolConstantProductParametersXDR.fromTxRep(map, prefix: "\(prefix).constantProduct")
      return .constantProduct(val)
    default:
      throw TxRepError.invalidValue(key: discKey)
    }
  }
}
