// This file was automatically generated by xdrgen.
// DO NOT EDIT or your changes may be overwritten.

import Foundation

public enum AccountMergeResultXDR: XDRCodable, Sendable {
  case sourceAccountBalance(Int64)
  case malformed
  case noAccount
  case immutableSet
  case hasSubEntries
  case seqnumTooFar
  case destFull
  case isSponsor

  public init(from decoder: Decoder) throws {
    var container = try decoder.unkeyedContainer()
    let discriminant = try container.decode(Int32.self)

    switch discriminant {
    case AccountMergeResultCode.success.rawValue:
      let val = try container.decode(Int64.self)
      self = .sourceAccountBalance(val)
    case AccountMergeResultCode.malformed.rawValue:
      self = .malformed
    case AccountMergeResultCode.noAccount.rawValue:
      self = .noAccount
    case AccountMergeResultCode.immutableSet.rawValue:
      self = .immutableSet
    case AccountMergeResultCode.hasSubEntries.rawValue:
      self = .hasSubEntries
    case AccountMergeResultCode.seqnumTooFar.rawValue:
      self = .seqnumTooFar
    case AccountMergeResultCode.destFull.rawValue:
      self = .destFull
    case AccountMergeResultCode.isSponsor.rawValue:
      self = .isSponsor
    default:
      throw StellarSDKError.xdrDecodingError(message: "Unknown AccountMergeResultXDR discriminant: \(discriminant)")
    }
  }

  public func type() -> Int32 {
    switch self {
    case .sourceAccountBalance: return AccountMergeResultCode.success.rawValue
    case .malformed: return AccountMergeResultCode.malformed.rawValue
    case .noAccount: return AccountMergeResultCode.noAccount.rawValue
    case .immutableSet: return AccountMergeResultCode.immutableSet.rawValue
    case .hasSubEntries: return AccountMergeResultCode.hasSubEntries.rawValue
    case .seqnumTooFar: return AccountMergeResultCode.seqnumTooFar.rawValue
    case .destFull: return AccountMergeResultCode.destFull.rawValue
    case .isSponsor: return AccountMergeResultCode.isSponsor.rawValue
    }
  }

  public func encode(to encoder: Encoder) throws {
    var container = encoder.unkeyedContainer()
    try container.encode(type())

    switch self {
    case .sourceAccountBalance(let val):
      try container.encode(val)
    case .malformed:
      break
    case .noAccount:
      break
    case .immutableSet:
      break
    case .hasSubEntries:
      break
    case .seqnumTooFar:
      break
    case .destFull:
      break
    case .isSponsor:
      break
    }
  }
}

extension AccountMergeResultXDR: XdrJsonCodable {
  public func toXdrJsonValue() throws -> XdrJsonValue {
    switch self {
    case .sourceAccountBalance(let payload):
      return .object([XdrJsonMember(key: "success", value: try Int64XDRJsonCodec.toXdrJsonValue(payload, type: "AccountMergeResultXDR", key: "success"))])
    case .malformed: return .string("malformed")
    case .noAccount: return .string("no_account")
    case .immutableSet: return .string("immutable_set")
    case .hasSubEntries: return .string("has_sub_entries")
    case .seqnumTooFar: return .string("seqnum_too_far")
    case .destFull: return .string("dest_full")
    case .isSponsor: return .string("is_sponsor")
    }
  }

  public static func fromXdrJsonValue(_ value: XdrJsonValue) throws -> AccountMergeResultXDR {
    if case .string(let name) = value {
      switch name {
      case "success":
        throw XdrJsonError.invalidValue(type: "AccountMergeResultXDR", key: "success",
                                        message: "this arm carries a value, so it is written as a single-key object")
      case "malformed":
        return .malformed
      case "no_account":
        return .noAccount
      case "immutable_set":
        return .immutableSet
      case "has_sub_entries":
        return .hasSubEntries
      case "seqnum_too_far":
        return .seqnumTooFar
      case "dest_full":
        return .destFull
      case "is_sponsor":
        return .isSponsor
      default:
        throw XdrJsonError.unknownUnionArm(type: "AccountMergeResultXDR", key: name)
      }
    }

    let member = try XdrJson.singleKeyObject(value, type: "AccountMergeResultXDR")
    switch member.key {
    case "success":
      let sourceAccountBalance: Int64 = try Int64XDRJsonCodec.fromXdrJsonValue(member.value, type: "AccountMergeResultXDR", key: "success")
      return .sourceAccountBalance(sourceAccountBalance)
    case "malformed":
      throw XdrJsonError.invalidValue(type: "AccountMergeResultXDR", key: "malformed",
                                      message: "this arm carries no value, so it is written as a bare string")
    case "no_account":
      throw XdrJsonError.invalidValue(type: "AccountMergeResultXDR", key: "no_account",
                                      message: "this arm carries no value, so it is written as a bare string")
    case "immutable_set":
      throw XdrJsonError.invalidValue(type: "AccountMergeResultXDR", key: "immutable_set",
                                      message: "this arm carries no value, so it is written as a bare string")
    case "has_sub_entries":
      throw XdrJsonError.invalidValue(type: "AccountMergeResultXDR", key: "has_sub_entries",
                                      message: "this arm carries no value, so it is written as a bare string")
    case "seqnum_too_far":
      throw XdrJsonError.invalidValue(type: "AccountMergeResultXDR", key: "seqnum_too_far",
                                      message: "this arm carries no value, so it is written as a bare string")
    case "dest_full":
      throw XdrJsonError.invalidValue(type: "AccountMergeResultXDR", key: "dest_full",
                                      message: "this arm carries no value, so it is written as a bare string")
    case "is_sponsor":
      throw XdrJsonError.invalidValue(type: "AccountMergeResultXDR", key: "is_sponsor",
                                      message: "this arm carries no value, so it is written as a bare string")
    default:
      throw XdrJsonError.unknownUnionArm(type: "AccountMergeResultXDR", key: member.key)
    }
  }
}
