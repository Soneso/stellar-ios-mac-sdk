// This file was automatically generated by xdrgen.
// DO NOT EDIT or your changes may be overwritten.

import Foundation

public enum StellarMessageXDR: XDRCodable, Sendable {
  case error(ErrorXDR)
  case hello(HelloXDR)
  case auth(AuthXDR)
  case dontHave(DontHaveXDR)
  case peers([PeerAddressXDR])
  case txSetHash(WrappedData32)
  case txSet(TransactionSetXDR)
  case generalizedTxSet(GeneralizedTransactionSetXDR)
  case transaction(TransactionEnvelopeXDR)
  case signedTimeSlicedSurveyRequestMessage(SignedTimeSlicedSurveyRequestMessageXDR)
  case signedTimeSlicedSurveyResponseMessage(SignedTimeSlicedSurveyResponseMessageXDR)
  case signedTimeSlicedSurveyStartCollectingMessage(SignedTimeSlicedSurveyStartCollectingMessageXDR)
  case signedTimeSlicedSurveyStopCollectingMessage(SignedTimeSlicedSurveyStopCollectingMessageXDR)
  case qSetHash(WrappedData32)
  case qSet(SCPQuorumSetXDR)
  case envelope(SCPEnvelopeXDR)
  case getSCPLedgerSeq(UInt32)
  case sendMoreMessage(SendMoreXDR)
  case sendMoreExtendedMessage(SendMoreExtendedXDR)
  case floodAdvert(FloodAdvertXDR)
  case floodDemand(FloodDemandXDR)

  public init(from decoder: Decoder) throws {
    var container = try decoder.unkeyedContainer()
    let discriminant = try container.decode(Int32.self)

    switch discriminant {
    case MessageTypeXDR.errorMsg.rawValue:
      let val = try container.decode(ErrorXDR.self)
      self = .error(val)
    case MessageTypeXDR.hello.rawValue:
      let val = try container.decode(HelloXDR.self)
      self = .hello(val)
    case MessageTypeXDR.auth.rawValue:
      let val = try container.decode(AuthXDR.self)
      self = .auth(val)
    case MessageTypeXDR.dontHave.rawValue:
      let val = try container.decode(DontHaveXDR.self)
      self = .dontHave(val)
    case MessageTypeXDR.peers.rawValue:
      let val = try decodeArray(type: PeerAddressXDR.self, dec: decoder)
      self = .peers(val)
    case MessageTypeXDR.getTxSet.rawValue:
      let val = try container.decode(WrappedData32.self)
      self = .txSetHash(val)
    case MessageTypeXDR.txSet.rawValue:
      let val = try container.decode(TransactionSetXDR.self)
      self = .txSet(val)
    case MessageTypeXDR.generalizedTxSet.rawValue:
      let val = try container.decode(GeneralizedTransactionSetXDR.self)
      self = .generalizedTxSet(val)
    case MessageTypeXDR.transaction.rawValue:
      let val = try container.decode(TransactionEnvelopeXDR.self)
      self = .transaction(val)
    case MessageTypeXDR.timeSlicedSurveyRequest.rawValue:
      let val = try container.decode(SignedTimeSlicedSurveyRequestMessageXDR.self)
      self = .signedTimeSlicedSurveyRequestMessage(val)
    case MessageTypeXDR.timeSlicedSurveyResponse.rawValue:
      let val = try container.decode(SignedTimeSlicedSurveyResponseMessageXDR.self)
      self = .signedTimeSlicedSurveyResponseMessage(val)
    case MessageTypeXDR.timeSlicedSurveyStartCollecting.rawValue:
      let val = try container.decode(SignedTimeSlicedSurveyStartCollectingMessageXDR.self)
      self = .signedTimeSlicedSurveyStartCollectingMessage(val)
    case MessageTypeXDR.timeSlicedSurveyStopCollecting.rawValue:
      let val = try container.decode(SignedTimeSlicedSurveyStopCollectingMessageXDR.self)
      self = .signedTimeSlicedSurveyStopCollectingMessage(val)
    case MessageTypeXDR.getScpQuorumset.rawValue:
      let val = try container.decode(WrappedData32.self)
      self = .qSetHash(val)
    case MessageTypeXDR.scpQuorumset.rawValue:
      let val = try container.decode(SCPQuorumSetXDR.self)
      self = .qSet(val)
    case MessageTypeXDR.scpMessage.rawValue:
      let val = try container.decode(SCPEnvelopeXDR.self)
      self = .envelope(val)
    case MessageTypeXDR.getScpState.rawValue:
      let val = try container.decode(UInt32.self)
      self = .getSCPLedgerSeq(val)
    case MessageTypeXDR.sendMore.rawValue:
      let val = try container.decode(SendMoreXDR.self)
      self = .sendMoreMessage(val)
    case MessageTypeXDR.sendMoreExtended.rawValue:
      let val = try container.decode(SendMoreExtendedXDR.self)
      self = .sendMoreExtendedMessage(val)
    case MessageTypeXDR.floodAdvert.rawValue:
      let val = try container.decode(FloodAdvertXDR.self)
      self = .floodAdvert(val)
    case MessageTypeXDR.floodDemand.rawValue:
      let val = try container.decode(FloodDemandXDR.self)
      self = .floodDemand(val)
    default:
      throw StellarSDKError.xdrDecodingError(message: "Unknown StellarMessageXDR discriminant: \(discriminant)")
    }
  }

  public func type() -> Int32 {
    switch self {
    case .error: return MessageTypeXDR.errorMsg.rawValue
    case .hello: return MessageTypeXDR.hello.rawValue
    case .auth: return MessageTypeXDR.auth.rawValue
    case .dontHave: return MessageTypeXDR.dontHave.rawValue
    case .peers: return MessageTypeXDR.peers.rawValue
    case .txSetHash: return MessageTypeXDR.getTxSet.rawValue
    case .txSet: return MessageTypeXDR.txSet.rawValue
    case .generalizedTxSet: return MessageTypeXDR.generalizedTxSet.rawValue
    case .transaction: return MessageTypeXDR.transaction.rawValue
    case .signedTimeSlicedSurveyRequestMessage: return MessageTypeXDR.timeSlicedSurveyRequest.rawValue
    case .signedTimeSlicedSurveyResponseMessage: return MessageTypeXDR.timeSlicedSurveyResponse.rawValue
    case .signedTimeSlicedSurveyStartCollectingMessage: return MessageTypeXDR.timeSlicedSurveyStartCollecting.rawValue
    case .signedTimeSlicedSurveyStopCollectingMessage: return MessageTypeXDR.timeSlicedSurveyStopCollecting.rawValue
    case .qSetHash: return MessageTypeXDR.getScpQuorumset.rawValue
    case .qSet: return MessageTypeXDR.scpQuorumset.rawValue
    case .envelope: return MessageTypeXDR.scpMessage.rawValue
    case .getSCPLedgerSeq: return MessageTypeXDR.getScpState.rawValue
    case .sendMoreMessage: return MessageTypeXDR.sendMore.rawValue
    case .sendMoreExtendedMessage: return MessageTypeXDR.sendMoreExtended.rawValue
    case .floodAdvert: return MessageTypeXDR.floodAdvert.rawValue
    case .floodDemand: return MessageTypeXDR.floodDemand.rawValue
    }
  }

  public func encode(to encoder: Encoder) throws {
    var container = encoder.unkeyedContainer()
    try container.encode(type())

    switch self {
    case .error(let val):
      try container.encode(val)
    case .hello(let val):
      try container.encode(val)
    case .auth(let val):
      try container.encode(val)
    case .dontHave(let val):
      try container.encode(val)
    case .peers(let val):
      try container.encode(val)
    case .txSetHash(let val):
      try container.encode(val)
    case .txSet(let val):
      try container.encode(val)
    case .generalizedTxSet(let val):
      try container.encode(val)
    case .transaction(let val):
      try container.encode(val)
    case .signedTimeSlicedSurveyRequestMessage(let val):
      try container.encode(val)
    case .signedTimeSlicedSurveyResponseMessage(let val):
      try container.encode(val)
    case .signedTimeSlicedSurveyStartCollectingMessage(let val):
      try container.encode(val)
    case .signedTimeSlicedSurveyStopCollectingMessage(let val):
      try container.encode(val)
    case .qSetHash(let val):
      try container.encode(val)
    case .qSet(let val):
      try container.encode(val)
    case .envelope(let val):
      try container.encode(val)
    case .getSCPLedgerSeq(let val):
      try container.encode(val)
    case .sendMoreMessage(let val):
      try container.encode(val)
    case .sendMoreExtendedMessage(let val):
      try container.encode(val)
    case .floodAdvert(let val):
      try container.encode(val)
    case .floodDemand(let val):
      try container.encode(val)
    }
  }
}
