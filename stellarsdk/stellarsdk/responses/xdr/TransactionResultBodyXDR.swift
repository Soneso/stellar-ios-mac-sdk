// This file was automatically generated by xdrgen.
// DO NOT EDIT or your changes may be overwritten.

import Foundation

public enum TransactionResultBodyXDR: XDRCodable, Sendable {
  case feeBumpInnerSuccess(InnerTransactionResultPair)
  case feeBumpInnerFailed(InnerTransactionResultPair)
  case success([OperationResultXDR])
  case failed([OperationResultXDR])
  case tooEarly
  case tooLate
  case missingOperation
  case badSeq
  case badAuth
  case insufficientBalance
  case noAccount
  case insufficientFee
  case badAuthExtra
  case internalError
  case notSupported
  case badSponsorship
  case badMinSeqAgeOrGap
  case malformed
  case sorobanInvalid
  case frozenKeyAccessed

  public init(from decoder: Decoder) throws {
    var container = try decoder.unkeyedContainer()
    let discriminant = try container.decode(Int32.self)

    switch discriminant {
    case TransactionResultCode.feeBumpInnerSuccess.rawValue:
      let val = try container.decode(InnerTransactionResultPair.self)
      self = .feeBumpInnerSuccess(val)
    case TransactionResultCode.feeBumpInnerFailed.rawValue:
      let val = try container.decode(InnerTransactionResultPair.self)
      self = .feeBumpInnerFailed(val)
    case TransactionResultCode.success.rawValue:
      let val = try decodeArray(type: OperationResultXDR.self, dec: decoder)
      self = .success(val)
    case TransactionResultCode.failed.rawValue:
      let val = try decodeArray(type: OperationResultXDR.self, dec: decoder)
      self = .failed(val)
    case TransactionResultCode.tooEarly.rawValue:
      self = .tooEarly
    case TransactionResultCode.tooLate.rawValue:
      self = .tooLate
    case TransactionResultCode.missingOperation.rawValue:
      self = .missingOperation
    case TransactionResultCode.badSeq.rawValue:
      self = .badSeq
    case TransactionResultCode.badAuth.rawValue:
      self = .badAuth
    case TransactionResultCode.insufficientBalance.rawValue:
      self = .insufficientBalance
    case TransactionResultCode.noAccount.rawValue:
      self = .noAccount
    case TransactionResultCode.insufficientFee.rawValue:
      self = .insufficientFee
    case TransactionResultCode.badAuthExtra.rawValue:
      self = .badAuthExtra
    case TransactionResultCode.internalError.rawValue:
      self = .internalError
    case TransactionResultCode.notSupported.rawValue:
      self = .notSupported
    case TransactionResultCode.badSponsorship.rawValue:
      self = .badSponsorship
    case TransactionResultCode.badMinSeqAgeOrGap.rawValue:
      self = .badMinSeqAgeOrGap
    case TransactionResultCode.malformed.rawValue:
      self = .malformed
    case TransactionResultCode.sorobanInvalid.rawValue:
      self = .sorobanInvalid
    case TransactionResultCode.frozenKeyAccessed.rawValue:
      self = .frozenKeyAccessed
    default:
      throw StellarSDKError.xdrDecodingError(message: "Unknown TransactionResultBodyXDR discriminant: \(discriminant)")
    }
  }

  public func type() -> Int32 {
    switch self {
    case .feeBumpInnerSuccess: return TransactionResultCode.feeBumpInnerSuccess.rawValue
    case .feeBumpInnerFailed: return TransactionResultCode.feeBumpInnerFailed.rawValue
    case .success: return TransactionResultCode.success.rawValue
    case .failed: return TransactionResultCode.failed.rawValue
    case .tooEarly: return TransactionResultCode.tooEarly.rawValue
    case .tooLate: return TransactionResultCode.tooLate.rawValue
    case .missingOperation: return TransactionResultCode.missingOperation.rawValue
    case .badSeq: return TransactionResultCode.badSeq.rawValue
    case .badAuth: return TransactionResultCode.badAuth.rawValue
    case .insufficientBalance: return TransactionResultCode.insufficientBalance.rawValue
    case .noAccount: return TransactionResultCode.noAccount.rawValue
    case .insufficientFee: return TransactionResultCode.insufficientFee.rawValue
    case .badAuthExtra: return TransactionResultCode.badAuthExtra.rawValue
    case .internalError: return TransactionResultCode.internalError.rawValue
    case .notSupported: return TransactionResultCode.notSupported.rawValue
    case .badSponsorship: return TransactionResultCode.badSponsorship.rawValue
    case .badMinSeqAgeOrGap: return TransactionResultCode.badMinSeqAgeOrGap.rawValue
    case .malformed: return TransactionResultCode.malformed.rawValue
    case .sorobanInvalid: return TransactionResultCode.sorobanInvalid.rawValue
    case .frozenKeyAccessed: return TransactionResultCode.frozenKeyAccessed.rawValue
    }
  }

  public func encode(to encoder: Encoder) throws {
    var container = encoder.unkeyedContainer()
    try container.encode(type())

    switch self {
    case .feeBumpInnerSuccess(let val):
      try container.encode(val)
    case .feeBumpInnerFailed(let val):
      try container.encode(val)
    case .success(let val):
      try container.encode(val)
    case .failed(let val):
      try container.encode(val)
    case .tooEarly:
      break
    case .tooLate:
      break
    case .missingOperation:
      break
    case .badSeq:
      break
    case .badAuth:
      break
    case .insufficientBalance:
      break
    case .noAccount:
      break
    case .insufficientFee:
      break
    case .badAuthExtra:
      break
    case .internalError:
      break
    case .notSupported:
      break
    case .badSponsorship:
      break
    case .badMinSeqAgeOrGap:
      break
    case .malformed:
      break
    case .sorobanInvalid:
      break
    case .frozenKeyAccessed:
      break
    }
  }
}

extension TransactionResultBodyXDR: XdrJsonCodable {
  public func toXdrJsonValue() throws -> XdrJsonValue {
    switch self {
    case .feeBumpInnerSuccess(let payload):
      return .object([XdrJsonMember(key: "tx_fee_bump_inner_success", value: try payload.toXdrJsonValue())])
    case .feeBumpInnerFailed(let payload):
      return .object([XdrJsonMember(key: "tx_fee_bump_inner_failed", value: try payload.toXdrJsonValue())])
    case .success(let payload):
      return .object([XdrJsonMember(key: "tx_success", value: try XdrJson.array(payload.map { element in try element.toXdrJsonValue() }))])
    case .failed(let payload):
      return .object([XdrJsonMember(key: "tx_failed", value: try XdrJson.array(payload.map { element in try element.toXdrJsonValue() }))])
    case .tooEarly: return .string("tx_too_early")
    case .tooLate: return .string("tx_too_late")
    case .missingOperation: return .string("tx_missing_operation")
    case .badSeq: return .string("tx_bad_seq")
    case .badAuth: return .string("tx_bad_auth")
    case .insufficientBalance: return .string("tx_insufficient_balance")
    case .noAccount: return .string("tx_no_account")
    case .insufficientFee: return .string("tx_insufficient_fee")
    case .badAuthExtra: return .string("tx_bad_auth_extra")
    case .internalError: return .string("tx_internal_error")
    case .notSupported: return .string("tx_not_supported")
    case .badSponsorship: return .string("tx_bad_sponsorship")
    case .badMinSeqAgeOrGap: return .string("tx_bad_min_seq_age_or_gap")
    case .malformed: return .string("tx_malformed")
    case .sorobanInvalid: return .string("tx_soroban_invalid")
    case .frozenKeyAccessed: return .string("tx_frozen_key_accessed")
    }
  }

  public static func fromXdrJsonValue(_ value: XdrJsonValue) throws -> TransactionResultBodyXDR {
    if case .string(let name) = value {
      switch name {
      case "tx_fee_bump_inner_success":
        throw XdrJsonError.invalidValue(type: "TransactionResultBodyXDR", key: "tx_fee_bump_inner_success",
                                        message: "this arm carries a value, so it is written as a single-key object")
      case "tx_fee_bump_inner_failed":
        throw XdrJsonError.invalidValue(type: "TransactionResultBodyXDR", key: "tx_fee_bump_inner_failed",
                                        message: "this arm carries a value, so it is written as a single-key object")
      case "tx_success":
        throw XdrJsonError.invalidValue(type: "TransactionResultBodyXDR", key: "tx_success",
                                        message: "this arm carries a value, so it is written as a single-key object")
      case "tx_failed":
        throw XdrJsonError.invalidValue(type: "TransactionResultBodyXDR", key: "tx_failed",
                                        message: "this arm carries a value, so it is written as a single-key object")
      case "tx_too_early":
        return .tooEarly
      case "tx_too_late":
        return .tooLate
      case "tx_missing_operation":
        return .missingOperation
      case "tx_bad_seq":
        return .badSeq
      case "tx_bad_auth":
        return .badAuth
      case "tx_insufficient_balance":
        return .insufficientBalance
      case "tx_no_account":
        return .noAccount
      case "tx_insufficient_fee":
        return .insufficientFee
      case "tx_bad_auth_extra":
        return .badAuthExtra
      case "tx_internal_error":
        return .internalError
      case "tx_not_supported":
        return .notSupported
      case "tx_bad_sponsorship":
        return .badSponsorship
      case "tx_bad_min_seq_age_or_gap":
        return .badMinSeqAgeOrGap
      case "tx_malformed":
        return .malformed
      case "tx_soroban_invalid":
        return .sorobanInvalid
      case "tx_frozen_key_accessed":
        return .frozenKeyAccessed
      default:
        throw XdrJsonError.unknownUnionArm(type: "TransactionResultBodyXDR", key: name)
      }
    }

    let member = try XdrJson.singleKeyObject(value, type: "TransactionResultBodyXDR")
    switch member.key {
    case "tx_fee_bump_inner_success":
      let feeBumpInnerSuccess: InnerTransactionResultPair = try InnerTransactionResultPair.fromXdrJsonValue(member.value)
      return .feeBumpInnerSuccess(feeBumpInnerSuccess)
    case "tx_fee_bump_inner_failed":
      let feeBumpInnerFailed: InnerTransactionResultPair = try InnerTransactionResultPair.fromXdrJsonValue(member.value)
      return .feeBumpInnerFailed(feeBumpInnerFailed)
    case "tx_success":
      let successElements = try XdrJson.array(member.value, type: "TransactionResultBodyXDR", key: "tx_success")
      let success: [OperationResultXDR] = try successElements.map { element in try OperationResultXDR.fromXdrJsonValue(element) }
      return .success(success)
    case "tx_failed":
      let failedElements = try XdrJson.array(member.value, type: "TransactionResultBodyXDR", key: "tx_failed")
      let failed: [OperationResultXDR] = try failedElements.map { element in try OperationResultXDR.fromXdrJsonValue(element) }
      return .failed(failed)
    case "tx_too_early":
      throw XdrJsonError.invalidValue(type: "TransactionResultBodyXDR", key: "tx_too_early",
                                      message: "this arm carries no value, so it is written as a bare string")
    case "tx_too_late":
      throw XdrJsonError.invalidValue(type: "TransactionResultBodyXDR", key: "tx_too_late",
                                      message: "this arm carries no value, so it is written as a bare string")
    case "tx_missing_operation":
      throw XdrJsonError.invalidValue(type: "TransactionResultBodyXDR", key: "tx_missing_operation",
                                      message: "this arm carries no value, so it is written as a bare string")
    case "tx_bad_seq":
      throw XdrJsonError.invalidValue(type: "TransactionResultBodyXDR", key: "tx_bad_seq",
                                      message: "this arm carries no value, so it is written as a bare string")
    case "tx_bad_auth":
      throw XdrJsonError.invalidValue(type: "TransactionResultBodyXDR", key: "tx_bad_auth",
                                      message: "this arm carries no value, so it is written as a bare string")
    case "tx_insufficient_balance":
      throw XdrJsonError.invalidValue(type: "TransactionResultBodyXDR", key: "tx_insufficient_balance",
                                      message: "this arm carries no value, so it is written as a bare string")
    case "tx_no_account":
      throw XdrJsonError.invalidValue(type: "TransactionResultBodyXDR", key: "tx_no_account",
                                      message: "this arm carries no value, so it is written as a bare string")
    case "tx_insufficient_fee":
      throw XdrJsonError.invalidValue(type: "TransactionResultBodyXDR", key: "tx_insufficient_fee",
                                      message: "this arm carries no value, so it is written as a bare string")
    case "tx_bad_auth_extra":
      throw XdrJsonError.invalidValue(type: "TransactionResultBodyXDR", key: "tx_bad_auth_extra",
                                      message: "this arm carries no value, so it is written as a bare string")
    case "tx_internal_error":
      throw XdrJsonError.invalidValue(type: "TransactionResultBodyXDR", key: "tx_internal_error",
                                      message: "this arm carries no value, so it is written as a bare string")
    case "tx_not_supported":
      throw XdrJsonError.invalidValue(type: "TransactionResultBodyXDR", key: "tx_not_supported",
                                      message: "this arm carries no value, so it is written as a bare string")
    case "tx_bad_sponsorship":
      throw XdrJsonError.invalidValue(type: "TransactionResultBodyXDR", key: "tx_bad_sponsorship",
                                      message: "this arm carries no value, so it is written as a bare string")
    case "tx_bad_min_seq_age_or_gap":
      throw XdrJsonError.invalidValue(type: "TransactionResultBodyXDR", key: "tx_bad_min_seq_age_or_gap",
                                      message: "this arm carries no value, so it is written as a bare string")
    case "tx_malformed":
      throw XdrJsonError.invalidValue(type: "TransactionResultBodyXDR", key: "tx_malformed",
                                      message: "this arm carries no value, so it is written as a bare string")
    case "tx_soroban_invalid":
      throw XdrJsonError.invalidValue(type: "TransactionResultBodyXDR", key: "tx_soroban_invalid",
                                      message: "this arm carries no value, so it is written as a bare string")
    case "tx_frozen_key_accessed":
      throw XdrJsonError.invalidValue(type: "TransactionResultBodyXDR", key: "tx_frozen_key_accessed",
                                      message: "this arm carries no value, so it is written as a bare string")
    default:
      throw XdrJsonError.unknownUnionArm(type: "TransactionResultBodyXDR", key: member.key)
    }
  }
}
