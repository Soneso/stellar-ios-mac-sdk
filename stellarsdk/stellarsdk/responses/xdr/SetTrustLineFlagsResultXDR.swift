// This file was automatically generated by xdrgen.
// DO NOT EDIT or your changes may be overwritten.

import Foundation

public enum SetTrustLineFlagsResultXDR: XDRCodable, Sendable {
  case success
  case malformed
  case noTrustLine
  case cantRevoke
  case invalidState
  case lowReserve

  public init(from decoder: Decoder) throws {
    var container = try decoder.unkeyedContainer()
    let discriminant = try container.decode(Int32.self)

    switch discriminant {
    case SetTrustLineFlagsResultCode.success.rawValue:
      self = .success
    case SetTrustLineFlagsResultCode.malformed.rawValue:
      self = .malformed
    case SetTrustLineFlagsResultCode.noTrustLine.rawValue:
      self = .noTrustLine
    case SetTrustLineFlagsResultCode.cantRevoke.rawValue:
      self = .cantRevoke
    case SetTrustLineFlagsResultCode.invalidState.rawValue:
      self = .invalidState
    case SetTrustLineFlagsResultCode.lowReserve.rawValue:
      self = .lowReserve
    default:
      throw StellarSDKError.xdrDecodingError(message: "Unknown SetTrustLineFlagsResultXDR discriminant: \(discriminant)")
    }
  }

  public func type() -> Int32 {
    switch self {
    case .success: return SetTrustLineFlagsResultCode.success.rawValue
    case .malformed: return SetTrustLineFlagsResultCode.malformed.rawValue
    case .noTrustLine: return SetTrustLineFlagsResultCode.noTrustLine.rawValue
    case .cantRevoke: return SetTrustLineFlagsResultCode.cantRevoke.rawValue
    case .invalidState: return SetTrustLineFlagsResultCode.invalidState.rawValue
    case .lowReserve: return SetTrustLineFlagsResultCode.lowReserve.rawValue
    }
  }

  public func encode(to encoder: Encoder) throws {
    var container = encoder.unkeyedContainer()
    try container.encode(type())

    switch self {
    case .success:
      break
    case .malformed:
      break
    case .noTrustLine:
      break
    case .cantRevoke:
      break
    case .invalidState:
      break
    case .lowReserve:
      break
    }
  }
}

extension SetTrustLineFlagsResultXDR: XdrJsonCodable {
  public func toXdrJsonValue() throws -> XdrJsonValue {
    switch self {
    case .success: return .string("success")
    case .malformed: return .string("malformed")
    case .noTrustLine: return .string("no_trust_line")
    case .cantRevoke: return .string("cant_revoke")
    case .invalidState: return .string("invalid_state")
    case .lowReserve: return .string("low_reserve")
    }
  }

  public static func fromXdrJsonValue(_ value: XdrJsonValue) throws -> SetTrustLineFlagsResultXDR {
    let name = try XdrJson.string(value, type: "SetTrustLineFlagsResultXDR")
    switch name {
    case "success":
      return .success
    case "malformed":
      return .malformed
    case "no_trust_line":
      return .noTrustLine
    case "cant_revoke":
      return .cantRevoke
    case "invalid_state":
      return .invalidState
    case "low_reserve":
      return .lowReserve
    default:
      throw XdrJsonError.unknownUnionArm(type: "SetTrustLineFlagsResultXDR", key: name)
    }
  }
}
