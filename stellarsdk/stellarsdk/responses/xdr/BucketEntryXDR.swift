// This file was automatically generated by xdrgen.
// DO NOT EDIT or your changes may be overwritten.

import Foundation

public enum BucketEntryXDR: XDRCodable, Sendable {
  case liveentry(LedgerEntryXDR)
  case initentry(LedgerEntryXDR)
  case deadEntry(LedgerKeyXDR)
  case metaEntry(BucketMetadataXDR)

  public init(from decoder: Decoder) throws {
    var container = try decoder.unkeyedContainer()
    let discriminant = try container.decode(Int32.self)

    switch discriminant {
    case BucketEntryTypeXDR.liveentry.rawValue:
      let val = try container.decode(LedgerEntryXDR.self)
      self = .liveentry(val)
    case BucketEntryTypeXDR.initentry.rawValue:
      let val = try container.decode(LedgerEntryXDR.self)
      self = .initentry(val)
    case BucketEntryTypeXDR.deadentry.rawValue:
      let val = try container.decode(LedgerKeyXDR.self)
      self = .deadEntry(val)
    case BucketEntryTypeXDR.metaentry.rawValue:
      let val = try container.decode(BucketMetadataXDR.self)
      self = .metaEntry(val)
    default:
      throw StellarSDKError.xdrDecodingError(message: "Unknown BucketEntryXDR discriminant: \(discriminant)")
    }
  }

  public func type() -> Int32 {
    switch self {
    case .liveentry: return BucketEntryTypeXDR.liveentry.rawValue
    case .initentry: return BucketEntryTypeXDR.initentry.rawValue
    case .deadEntry: return BucketEntryTypeXDR.deadentry.rawValue
    case .metaEntry: return BucketEntryTypeXDR.metaentry.rawValue
    }
  }

  public func encode(to encoder: Encoder) throws {
    var container = encoder.unkeyedContainer()
    try container.encode(type())

    switch self {
    case .liveentry(let val):
      try container.encode(val)
    case .initentry(let val):
      try container.encode(val)
    case .deadEntry(let val):
      try container.encode(val)
    case .metaEntry(let val):
      try container.encode(val)
    }
  }
}
