// This file was automatically generated by xdrgen.
// DO NOT EDIT or your changes may be overwritten.

import Foundation

public enum LedgerKeyXDR: XDRCodable, Sendable {
  case account(LedgerKeyAccountXDR)
  case trustline(LedgerKeyTrustLineXDR)
  case offer(LedgerKeyOfferXDR)
  case data(LedgerKeyDataXDR)
  case claimableBalance(LedgerKeyClaimableBalanceXDR)
  case liquidityPool(LedgerKeyLiquidityPoolXDR)
  case contractData(LedgerKeyContractDataXDR)
  case contractCode(LedgerKeyContractCodeXDR)
  case configSetting(LedgerKeyConfigSettingXDR)
  case ttl(LedgerKeyTTLXDR)

  public init(from decoder: Decoder) throws {
    var container = try decoder.unkeyedContainer()
    let discriminant = try container.decode(Int32.self)

    switch discriminant {
    case LedgerEntryType.account.rawValue:
      let val = try container.decode(LedgerKeyAccountXDR.self)
      self = .account(val)
    case LedgerEntryType.trustline.rawValue:
      let val = try container.decode(LedgerKeyTrustLineXDR.self)
      self = .trustline(val)
    case LedgerEntryType.offer.rawValue:
      let val = try container.decode(LedgerKeyOfferXDR.self)
      self = .offer(val)
    case LedgerEntryType.data.rawValue:
      let val = try container.decode(LedgerKeyDataXDR.self)
      self = .data(val)
    case LedgerEntryType.claimableBalance.rawValue:
      let val = try container.decode(LedgerKeyClaimableBalanceXDR.self)
      self = .claimableBalance(val)
    case LedgerEntryType.liquidityPool.rawValue:
      let val = try container.decode(LedgerKeyLiquidityPoolXDR.self)
      self = .liquidityPool(val)
    case LedgerEntryType.contractData.rawValue:
      let val = try container.decode(LedgerKeyContractDataXDR.self)
      self = .contractData(val)
    case LedgerEntryType.contractCode.rawValue:
      let val = try container.decode(LedgerKeyContractCodeXDR.self)
      self = .contractCode(val)
    case LedgerEntryType.configSetting.rawValue:
      let val = try container.decode(LedgerKeyConfigSettingXDR.self)
      self = .configSetting(val)
    case LedgerEntryType.ttl.rawValue:
      let val = try container.decode(LedgerKeyTTLXDR.self)
      self = .ttl(val)
    default:
      throw StellarSDKError.xdrDecodingError(message: "Unknown LedgerKeyXDR discriminant: \(discriminant)")
    }
  }

  public func type() -> Int32 {
    switch self {
    case .account: return LedgerEntryType.account.rawValue
    case .trustline: return LedgerEntryType.trustline.rawValue
    case .offer: return LedgerEntryType.offer.rawValue
    case .data: return LedgerEntryType.data.rawValue
    case .claimableBalance: return LedgerEntryType.claimableBalance.rawValue
    case .liquidityPool: return LedgerEntryType.liquidityPool.rawValue
    case .contractData: return LedgerEntryType.contractData.rawValue
    case .contractCode: return LedgerEntryType.contractCode.rawValue
    case .configSetting: return LedgerEntryType.configSetting.rawValue
    case .ttl: return LedgerEntryType.ttl.rawValue
    }
  }

  public func encode(to encoder: Encoder) throws {
    var container = encoder.unkeyedContainer()
    try container.encode(type())

    switch self {
    case .account(let val):
      try container.encode(val)
    case .trustline(let val):
      try container.encode(val)
    case .offer(let val):
      try container.encode(val)
    case .data(let val):
      try container.encode(val)
    case .claimableBalance(let val):
      try container.encode(val)
    case .liquidityPool(let val):
      try container.encode(val)
    case .contractData(let val):
      try container.encode(val)
    case .contractCode(let val):
      try container.encode(val)
    case .configSetting(let val):
      try container.encode(val)
    case .ttl(let val):
      try container.encode(val)
    }
  }
}

extension LedgerKeyXDR {
  public func toTxRep(prefix: String, lines: inout [String]) throws {
    switch self {
    case .account(let val):
      lines.append("\(prefix).type: ACCOUNT")
      try val.toTxRep(prefix: "\(prefix).account", lines: &lines)
    case .trustline(let val):
      lines.append("\(prefix).type: TRUSTLINE")
      try val.toTxRep(prefix: "\(prefix).trustLine", lines: &lines)
    case .offer(let val):
      lines.append("\(prefix).type: OFFER")
      try val.toTxRep(prefix: "\(prefix).offer", lines: &lines)
    case .data(let val):
      lines.append("\(prefix).type: DATA")
      try val.toTxRep(prefix: "\(prefix).data", lines: &lines)
    case .claimableBalance(let val):
      lines.append("\(prefix).type: CLAIMABLE_BALANCE")
      try val.toTxRep(prefix: "\(prefix).claimableBalance", lines: &lines)
    case .liquidityPool(let val):
      lines.append("\(prefix).type: LIQUIDITY_POOL")
      try val.toTxRep(prefix: "\(prefix).liquidityPool", lines: &lines)
    case .contractData(let val):
      lines.append("\(prefix).type: CONTRACT_DATA")
      try val.toTxRep(prefix: "\(prefix).contractData", lines: &lines)
    case .contractCode(let val):
      lines.append("\(prefix).type: CONTRACT_CODE")
      try val.toTxRep(prefix: "\(prefix).contractCode", lines: &lines)
    case .configSetting(let val):
      lines.append("\(prefix).type: CONFIG_SETTING")
      try val.toTxRep(prefix: "\(prefix).configSetting", lines: &lines)
    case .ttl(let val):
      lines.append("\(prefix).type: TTL")
      try val.toTxRep(prefix: "\(prefix).ttl", lines: &lines)
    }
  }

  public static func fromTxRep(_ map: [String: String], prefix: String) throws -> LedgerKeyXDR {
    let discKey = "\(prefix).type"
    guard let discName = TxRepHelper.getValue(map, discKey) else {
      throw TxRepError.missingValue(key: discKey)
    }
    switch discName {
    case "ACCOUNT":
      let val = try LedgerKeyAccountXDR.fromTxRep(map, prefix: "\(prefix).account")
      return .account(val)
    case "TRUSTLINE":
      let val = try LedgerKeyTrustLineXDR.fromTxRep(map, prefix: "\(prefix).trustLine")
      return .trustline(val)
    case "OFFER":
      let val = try LedgerKeyOfferXDR.fromTxRep(map, prefix: "\(prefix).offer")
      return .offer(val)
    case "DATA":
      let val = try LedgerKeyDataXDR.fromTxRep(map, prefix: "\(prefix).data")
      return .data(val)
    case "CLAIMABLE_BALANCE":
      let val = try LedgerKeyClaimableBalanceXDR.fromTxRep(map, prefix: "\(prefix).claimableBalance")
      return .claimableBalance(val)
    case "LIQUIDITY_POOL":
      let val = try LedgerKeyLiquidityPoolXDR.fromTxRep(map, prefix: "\(prefix).liquidityPool")
      return .liquidityPool(val)
    case "CONTRACT_DATA":
      let val = try LedgerKeyContractDataXDR.fromTxRep(map, prefix: "\(prefix).contractData")
      return .contractData(val)
    case "CONTRACT_CODE":
      let val = try LedgerKeyContractCodeXDR.fromTxRep(map, prefix: "\(prefix).contractCode")
      return .contractCode(val)
    case "CONFIG_SETTING":
      let val = try LedgerKeyConfigSettingXDR.fromTxRep(map, prefix: "\(prefix).configSetting")
      return .configSetting(val)
    case "TTL":
      let val = try LedgerKeyTTLXDR.fromTxRep(map, prefix: "\(prefix).ttl")
      return .ttl(val)
    default:
      throw TxRepError.invalidValue(key: discKey)
    }
  }
}

extension LedgerKeyXDR: XdrJsonCodable {
  public func toXdrJsonValue() throws -> XdrJsonValue {
    switch self {
    case .account(let payload):
      return .object([XdrJsonMember(key: "account", value: try payload.toXdrJsonValue())])
    case .trustline(let payload):
      return .object([XdrJsonMember(key: "trustline", value: try payload.toXdrJsonValue())])
    case .offer(let payload):
      return .object([XdrJsonMember(key: "offer", value: try payload.toXdrJsonValue())])
    case .data(let payload):
      return .object([XdrJsonMember(key: "data", value: try payload.toXdrJsonValue())])
    case .claimableBalance(let payload):
      return .object([XdrJsonMember(key: "claimable_balance", value: try payload.toXdrJsonValue())])
    case .liquidityPool(let payload):
      return .object([XdrJsonMember(key: "liquidity_pool", value: try payload.toXdrJsonValue())])
    case .contractData(let payload):
      return .object([XdrJsonMember(key: "contract_data", value: try payload.toXdrJsonValue())])
    case .contractCode(let payload):
      return .object([XdrJsonMember(key: "contract_code", value: try payload.toXdrJsonValue())])
    case .configSetting(let payload):
      return .object([XdrJsonMember(key: "config_setting", value: try payload.toXdrJsonValue())])
    case .ttl(let payload):
      return .object([XdrJsonMember(key: "ttl", value: try payload.toXdrJsonValue())])
    }
  }

  public static func fromXdrJsonValue(_ value: XdrJsonValue) throws -> LedgerKeyXDR {
    if case .string(let name) = value {
      switch name {
      case "account":
        throw XdrJsonError.invalidValue(type: "LedgerKeyXDR", key: "account",
                                        message: "this arm carries a value, so it is written as a single-key object")
      case "trustline":
        throw XdrJsonError.invalidValue(type: "LedgerKeyXDR", key: "trustline",
                                        message: "this arm carries a value, so it is written as a single-key object")
      case "offer":
        throw XdrJsonError.invalidValue(type: "LedgerKeyXDR", key: "offer",
                                        message: "this arm carries a value, so it is written as a single-key object")
      case "data":
        throw XdrJsonError.invalidValue(type: "LedgerKeyXDR", key: "data",
                                        message: "this arm carries a value, so it is written as a single-key object")
      case "claimable_balance":
        throw XdrJsonError.invalidValue(type: "LedgerKeyXDR", key: "claimable_balance",
                                        message: "this arm carries a value, so it is written as a single-key object")
      case "liquidity_pool":
        throw XdrJsonError.invalidValue(type: "LedgerKeyXDR", key: "liquidity_pool",
                                        message: "this arm carries a value, so it is written as a single-key object")
      case "contract_data":
        throw XdrJsonError.invalidValue(type: "LedgerKeyXDR", key: "contract_data",
                                        message: "this arm carries a value, so it is written as a single-key object")
      case "contract_code":
        throw XdrJsonError.invalidValue(type: "LedgerKeyXDR", key: "contract_code",
                                        message: "this arm carries a value, so it is written as a single-key object")
      case "config_setting":
        throw XdrJsonError.invalidValue(type: "LedgerKeyXDR", key: "config_setting",
                                        message: "this arm carries a value, so it is written as a single-key object")
      case "ttl":
        throw XdrJsonError.invalidValue(type: "LedgerKeyXDR", key: "ttl",
                                        message: "this arm carries a value, so it is written as a single-key object")
      default:
        throw XdrJsonError.unknownUnionArm(type: "LedgerKeyXDR", key: name)
      }
    }

    let member = try XdrJson.singleKeyObject(value, type: "LedgerKeyXDR")
    switch member.key {
    case "account":
      let account: LedgerKeyAccountXDR = try LedgerKeyAccountXDR.fromXdrJsonValue(member.value)
      return .account(account)
    case "trustline":
      let trustline: LedgerKeyTrustLineXDR = try LedgerKeyTrustLineXDR.fromXdrJsonValue(member.value)
      return .trustline(trustline)
    case "offer":
      let offer: LedgerKeyOfferXDR = try LedgerKeyOfferXDR.fromXdrJsonValue(member.value)
      return .offer(offer)
    case "data":
      let data: LedgerKeyDataXDR = try LedgerKeyDataXDR.fromXdrJsonValue(member.value)
      return .data(data)
    case "claimable_balance":
      let claimableBalance: LedgerKeyClaimableBalanceXDR = try LedgerKeyClaimableBalanceXDR.fromXdrJsonValue(member.value)
      return .claimableBalance(claimableBalance)
    case "liquidity_pool":
      let liquidityPool: LedgerKeyLiquidityPoolXDR = try LedgerKeyLiquidityPoolXDR.fromXdrJsonValue(member.value)
      return .liquidityPool(liquidityPool)
    case "contract_data":
      let contractData: LedgerKeyContractDataXDR = try LedgerKeyContractDataXDR.fromXdrJsonValue(member.value)
      return .contractData(contractData)
    case "contract_code":
      let contractCode: LedgerKeyContractCodeXDR = try LedgerKeyContractCodeXDR.fromXdrJsonValue(member.value)
      return .contractCode(contractCode)
    case "config_setting":
      let configSetting: LedgerKeyConfigSettingXDR = try LedgerKeyConfigSettingXDR.fromXdrJsonValue(member.value)
      return .configSetting(configSetting)
    case "ttl":
      let ttl: LedgerKeyTTLXDR = try LedgerKeyTTLXDR.fromXdrJsonValue(member.value)
      return .ttl(ttl)
    default:
      throw XdrJsonError.unknownUnionArm(type: "LedgerKeyXDR", key: member.key)
    }
  }
}
